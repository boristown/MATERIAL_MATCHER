# 批量部署证据 — 1.3.4→1.3.8（#116/#118/#119/#120/#121 + seed 初始设置 + 验收工具修复）

日期：2026-09-20。基线：1.3.3 → 最终 **1.3.8**，main 最终 `7ba34aa`（1.3.8 版本串 bump，代码基 `99ba2ae`）。**#104 全程未合并。**

## 合并序列（按指定顺序，每步同步 main）
- #116（Export Profile 抽象，CI 双绿）→ `9febde0`
- #118（自动推荐严格 1:1 + 拖拽/点线删除）同步 main 后 CI 双绿 → `7c16b14`
- #121（ReviewView 全状态 server-side 分页 + TopN 懒加载，含十万级后端测试）→ `dbb0783`（十万级测试按指示不再等待，理论兼容 + 冒烟为准）
- #119（STEP3 Top5 卡片交互 + 关键字段不一致/值映射未配置文案）同步无冲突（与 #121 契约断言自动双保留，已核 4+2 组断言在位，构建绿）→ `b9aac80`
- #120（实时任务 COMPLETED 后统一 router.push /review?task=，与 #118 同文件 TaskWorkspaceBase.vue 自动合并成功，双功能均确认在位）→ `99ba2ae`

## seed 初始设置（PR #117，1.3.4）
- 五子方案预置"国产进口"exact 规则（权重 0、value_mapping 10→国产/11→进口，可人工调整）；A007 文档转 composite（冻结 5 子方案版本，v4）；契约测试新增并全过（本地 pytest 262 passed）
- 既有环境（线上 + 客户机）经应用服务 save_draft+publish 发布新版本获得同样初始设置（不改历史版本；A007 双侧均实测 kind=composite children=5）
- 初始干净：全新安装不含任何任务/草稿（演练实证 STEP2 为空）；线上历史遗留 20 个 run-* 垃圾草稿已清

## 验收工具修复（#122/#123/#124，1.3.5→1.3.7）
- A007 组合方案端到端：按 composite_children 依序试跑；子方案规则与烟测数据列不符时按表头交集自适应重试（stdlib 多工作表 xlsx 表头解析）

## 部署
- 线上 mat.bjlzc.cn：1.3.3→1.3.8（中间 1.3.4-1.3.6 部分轮次被中断未装，最终态一致）；install.sh rc=0，端口 18160/密码 existing/种子幂等；dist `index-B3t8zKj8.js`（/review、关键字段不一致、值映射未配置均实测在位）；previous→1.3.6；备份 /oracle …/backups/backup-before-seed-134-20260920/
- 客户机 mm-customer：`/root/d` 一键 1.3.6→1.3.8 rc=0；health 1.3.8；密码 hash `6d224127`/6 方案/1 词表不变；mat2:18080→200
- 介质 **mm-1.3.8**：tar 1,238,573,190 B，SHA `63d0ebe5c89da900a81802e8636a43f9fff89bbd58d8cc7552fcb95f72e25cd6`，镜像 1.3.8（record b6027edf）；runtime/wheel/模型/Docker/浏览器全复用

## 运维记录
- btrfs 根卷曾被镜像构建泄漏占满（/tmp/mm-image-build-* ×23 ≈36G + 坏镜像 tar 4×1.2G，均为本专项测试残留）：已全部回收，/tmp 从 100% 降至 80%；后续构建 TMPDIR 改 .scratch 并即用即清；其它专项文件未动
