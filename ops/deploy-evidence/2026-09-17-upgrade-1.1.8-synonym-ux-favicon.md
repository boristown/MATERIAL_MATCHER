# 部署证据 — 1.1.8 升级(#47 同义词配置 UX 重构 + 品牌 favicon)

日期:2026-09-17(UTC+8)。性质:升级(承接 1.1.7 报告)。

## 1. #47 流程

- 分支 `feat/synonym-ux-favicon-20260917` 从当时最新 main `724786b`(1.1.7)创建;开发期间 main 未前进,mergeState=CLEAN。
- PR **#47** 审查:非 draft、CI backend+frontend 全绿(run 35214712993)、范围 13 文件(后端 5 均为同义词 API/文案/迁移,前端 7,新增 favicon.svg 与契约脚本),未触碰 matcher/召回/评分/STEP3 核心/STEP4/安装器/Docker。
- 按要求直接 squash 合并 → **`9f293b6`**;版本 chore → **`3a8c76c`(main,版本 1.1.8)**。

## 2. 功能落地(对照专项要求)

- 菜单/主标题统一「同义词配置」;页面与系统设置不再出现 业务字典/数据字典/字典管理/创建新版本(契约测试禁词锁定)。
- 主页面行内维护:固定四列网格 `原始写法 | → | 统一写法 | 操作`(minmax 等宽输入、固定箭头/操作列),sticky 表头、页面自然滚动、前端搜索、`+ 添加一条`、一次「保存修改」。
- 版本语义下沉:保存内部自动 create version,提示「保存后将形成新的同义词版本,历史任务不会受到影响」→「已保存为第 N 版」;dirty 提示「有未保存的修改」+ 放弃修改。
- 历史保留只读:版本/映射条数/大小写/创建账号/创建时间 + 展开只读详情;无修改历史入口。
- 区分大小写并入「匹配设置」区,带辅助说明。
- 管理员高级信息区未扩大(保持折叠)。
- 后端最小兼容:versions API 可选 `base_version_no`(缺省行为不变;过期基线 409 `DICTIONARY_VERSION_CONFLICT`);`dictionary_versions.created_by` 前向迁移;文案统一「同义词」。

## 3. 升级与验证

- 备份(pre-1.1.8,仓外):DB online backup(integrity ok)+ /etc 配置 tar;基线清点(tasks=36, dictionaries=1×20, profile v2×6)。
- 交付链:build_release → bundle 1.1.8(**10,827 文件**,+favicon.svg)→ verify(无 skip-arch)ok → manifest SHA `7a70f20b2a0371949b3fe73811010fa90180f3369a4e0069b45f05780445f2bb`(`bundle-118.sha`)→ install.sh 原子升级,未回滚。
- `current→1.1.8`,`previous→1.1.7`。
- 线上验证:
  - `https://mat.bjlzc.cn/favicon.svg` 200 `image/svg+xml`(小罡AI 红蓝火焰,显式尺寸);`/` 返回 `<title>物料集团码智能匹配平台</title>` + `rel="icon"`;部署 JS 含「同义词配置」、不含「创建新版本」
  - 服务重启自动完成 `created_by` 迁移(列存在);既有 20 条同义词 v1 完整;6 个方案 v2 的冻结引用不受影响
  - 并发探针:过期 `base_version_no=999` → 409 `DICTIONARY_VERSION_CONFLICT`(current=1),**零副作用**(仍 v1×20)
  - STEP3 五状态/selection-count/人工 Excel/直方图 回归通过;STEP4 结果 Excel 可读
  - doctor `ok=true version=1.1.8`,readiness 4/4;数据保留终检:无表缩小
- 前端质量门:test:browser-guard / test:review-contract / **test:synonym-contract(新)** / vue-tsc / vite 全过;后端 pytest **144 passed**;check_repo_files OK(SVG 合规,无二进制图标)。

## 4. 回滚点

`previous→releases/1.1.7`;bundle-117 `e91ae50a…` / bundle-118 `7a70f20b…`;DB/配置 pre-1.1.8 备份。#47 无 schema 破坏性变更(created_by 仅新增列),回滚 1.1.7 兼容。

## 5. 人工 smoke 待办(需浏览器肉眼)

菜单/页签图标、20 条直编、增删改→保存成版→刷新、搜索、历史只读展开、双窗口并发 409 提示、200+ 条 sticky 对齐、Chrome/Firefox 布局 —— 清单见 PR #47 描述。
