# 运维部署证据 — 1.1.5 测试环境升级(mat.bjlzc.cn)

日期:2026-09-17(UTC+8)  执行:ECC 主机运维 Agent  性质:**升级**(非全新安装)

## 1. 本轮合并(顺序:#37 → #40 → #41,squash)

| PR | 标题 | merge commit | 合并前检查 |
| --- | --- | --- | --- |
| #37 | fix(acceptance): gate calibrated review rate | `c9c4b5c260f718eb8252fe50b27bbe92b3baed30` | 非 draft / MERGEABLE+CLEAN / CI backend+frontend 绿 / 仅 2 个文本文件 |
| #40 | feat(review): scale manual decisions to filtered bulk operations | `0dea99256c6829de86610ef7cbac1b92f8a78ebf` | #37 合并后重判 CLEAN / CI 绿(head `293fc71`)/ 仅 app.py+新服务+测试 |
| #41 | feat(review-ui): optimize STEP3 for large-scale manual matching | `44bb8a8f7a9f2bf15414d365ba806adf466656ac` | #40 合并后重判 CLEAN / CI 绿(head `e5a9afe`)/ 仅 web/ |

- 未发生任何冲突,未手工改代码。
- PR #38(旧基线 E2E 证据)**有意保留为 OPEN**,未合并,待集成冻结后重跑正式 1000 行 E2E。
- 版本 chore 提交:`af5507f` → **最终 main commit / 最终版本 1.1.5**(pyproject+`__init__` 一致,builder 强制)。
- 合并后 main 全量验证:后端 `pytest` **137 passed**;前端 `npm run build`(含 browser-guard + 新增 review-contract 门禁)通过。

## 2. main 能力核对(对照本轮要求)

- 验收:`current_review_rate` 优先、旧评测回退 `review_rate`、缺值计入 missing_metrics → **BLOCKED(不当 0)**、evidence 输出 `effective_review_rate`(`acceptance_service.py`)。
- STEP3 后端:`/workbench/items` 五状态 ALL/MATCHED/REVIEW/CONFIRMED/UNMATCHED + `q/page/page_size(≤200)/include_candidates(0..5)` + first/second score、gap、critical_conflict 服务端筛选;selection 双模式 `explicit`(逐行 expected_version)/`filter`(仅提交筛选条件);`/workbench/bulk` 五动作 CONFIRM_TOP1/MARK_UNMATCHED/CANCEL_MATCH/CANCEL_UNMATCHED/RESTORE_ALGORITHM;`/workbench/decisions` 支持 Top1~Top5 `target_rank`/group/unmatched+REMATCH;`updated_at` 乐观锁、`VERSION_CONFLICT`、append-only `match_operation_logs/reviews/audit_events`;`BEGIN IMMEDIATE` 单事务 + 500 分块 + `executemany`;Excel 空值跳过/同值幂等/异值冲突不覆盖;旧单行/批量路由兼容适配。
- STEP3 前端:50/100/200 分页、五状态 tab、server-side 搜索/筛选、"选择当前筛选出的全部 N 条"仅发 filter、Top1~Top5 行内操作、一致/部分一致/不一致/无数据、双阈值(自动匹配阈值/人工处理下限)预览、Top1 与 Top1/Top2 分差直方图、人工匹配 Excel 下载/上传;构建期契约测试禁止"关键字段/为什么系统犹豫/风险/扣分项"等术语回潮。

## 3. 升级前(基线)

- 部署 commit `649fb4a`,版本 **1.1.4**,`current→releases/1.1.4`、`previous→releases/1.1.3`,bundle SHA `c6d2514b…`。
- 服务 `material_matcher.service` active(uvicorn 127.0.0.1:18160),`/api/health` `{"status":"ok","version":"1.1.4"}`,ready 全 true;隧道 `frpc-material-matcher.service` active(未触碰基础 frpc.service)。
- 数据:`MATERIAL_MATCHER_DATA_DIR=/var/lib/material_matcher → /oracle/material_matcher_data`(XFS);metadata DB `/var/lib/material_matcher/meta/material_matcher.db`(1.8 GB,WAL)。
- 备份(保留于主机 `/oracle/codex/work/MATERIAL/deploy-1.1.5-20260917/backup/`,不入 Git):
  - `material_matcher.db.pre-1.1.5`(sqlite3 online backup,`PRAGMA integrity_check=ok`)
  - `config-etc-material_matcher.pre-1.1.5.tar.gz`(0600)

## 4. 升级过程(复用仓库正式交付链,未新发明流程)

`build_release(1.1.5, 复用 runtime/wheelhouse/model,依赖零变更)` → `build_offline_bundle`(10,826 文件)→ `verify_offline_bundle.py`(无 --skip-arch,ok=true)→ bundle SHA `47ee9daf9346721872300084f07208f6e191a1d50149095f646bdc1d5509ca21` → `MATERIAL_MATCHER_ALLOW_UNSUPPORTED_OS=1 ./install.sh`(校验→版本化复制→旧服务在线跑新版本严格 doctor→停旧→原子切换→启动→readiness;失败自动回滚)→ **一次成功,未触发 rollback**。

- 唯一波折:首次执行 install.sh 时系统 `python3`(SLES 3.4)无法解析 f-string 而**在预校验阶段即安全终止**(无任何副作用);改用既有 `release-build/shim/python3`(3.11.16)后成功。

## 5. 升级后验证(2026-09-17,全部对真实部署实例执行)

- 基础:health `version=1.1.5`、ready 全 true、`https://mat.bjlzc.cn/api/health` 公网 200 `1.1.5`、前端 index/assets 200、登录/session 刷新保持、`doctor --require-frontend --require-embedding --require-release-manifest` ok=true、tasks=32(升级后新增 1 个冒烟任务=33,历史全在)。
- STEP1~STEP2:既有 10 万行任务 `SCALE-100Kx1M-C` 打开/统计正常;结果 Excel 下载 16.2 MB 正常;新冒烟任务以**双 Excel(xlsx)上传→草稿→dry-run→正式任务 COMPLETED** 全链通过。
- STEP3 实测(冒烟任务,1000 行):
  - 五状态服务端筛选/分页 50/100/200/搜索/Top5 内嵌 ✓
  - **server-side filter → selection-count N=711 → bulk CONFIRM_TOP1(请求体仅 169 字节,不含任何 ID)** ✓;既有 10 万任务 `selection-count(MATCHED)=80,298` 同样零 ID 传输 ✓
  - 显式选择带 expected_version 批量 CANCEL_MATCH ✓;伪造版本 → `VERSION_CONFLICT` ✓;显式缺版本 → 422 `EXPECTED_VERSION_REQUIRED` ✓
  - Top1~Top5 逐行 decisions、REMATCH、标记均不匹配、取消未匹配、恢复算法原始(RESTORE_ALGORITHM filter)✓
  - 双阈值 re-decide preview(不改数据)→ apply;人工已处理行不被阈值重判覆盖 ✓;calibration 返回 `top1_score_histogram`+`top1_top2_gap_histogram` ✓
  - Excel 下载(1000 行模板)→ 上传:成功应用/空白跳过/同文件重复相同=幂等跳过/同文件重复不同=`DUPLICATE_ROW_CONFLICT` 双方不落库/与已有手工结果不同=`MANUAL_RESULT_CONFLICT` 不覆盖 ✓
  - reviewer 角色对 `/workbench/bulk` 权限扩展生效(临时账号实测 200,验证后已停用)✓
- #37 门禁判别(部署代码、只读、临时阈值 env,不改动 `/etc`):同一评测 `review_rate=0.841`、`current_review_rate=0.714`,上限 0.75 → gate=PASS 且 `effective_review_rate=0.714`(旧口径会 FAIL)——证明按校准后口径把关、缺值 BLOCKED。
- `acceptance` CLI(1.1.5):**5 PASS / 4 BLOCKED / 0 FAIL,code_ready=true**;BLOCKED 为既有外部证据项(项目阈值 acceptance.env、真实百万金标、麒麟实机),与部署报告一致,非本轮回归。
- 审计:audit_events/reviews/match_operation_logs 仅追加,升级前后无任何丢失。

## 6. 数据保留终检(pre-backup vs live)

所有历史表只增不减;全部 32 个旧任务与 `SCALE-100Kx1M-C` 的 100,000 行逐项比对不变;未清空任务/评测/人工日志;未重置管理员密码;未 revoke 既有用户会话;原始上传文件未动。

## 7. 回滚点

- 代码:`/opt/material_matcher/previous→releases/1.1.4` + bundle `material-matcher-1.1.4-x86_64`(重跑 install.sh 即原子回退);1.1.5 bundle SHA 已登记(§4)。
- 数据:§3 备份(DB+配置)。回滚前须评估 `1.1.5` 新增手工操作记录的数据兼容性(仅追加,预期兼容)。

## 8. 遗留问题

1. Vault `accounts.env` 中四账号口令与线上实际口令不一致(admin 以 bootstrap 文件口令可登录)——属凭据簿维护问题,未改动任何账号口令,待管理员线下更新。
2. `current_review_rate` 端到端金标仍 BLOCKED(缺 `/etc/material_matcher/acceptance.env` 项目批准阈值与真实百万金标,§生产验收仍缺不变)。
3. 本轮部署证据由脚本发起的冒烟任务 `SMOKE-1.1.5`、`smoke_reviewer_115`(已停用)留存于测试环境,作为测试历史保留。
4. 最终 1000 行正式 E2E(#38)与 GPT5 部署专项、离线介质制作按计划在集成冻结后另行执行。
