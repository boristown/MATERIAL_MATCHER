# 部署证据 — MATERIAL_MATCHER 测试环境升级 1.1.4 → 1.1.5(mat.bjlzc.cn)

日期:2026-09-17(UTC+8)。性质:**升级**,非全新安装;未新建环境、未切换 Native/Docker、未触碰数据目录。

## 1. 本轮合并(顺序 #37 → #40 → #41,squash,无冲突,未手工解决任何冲突)

| PR | 标题 | merge commit | 合并前检查 |
| --- | --- | --- | --- |
| #37 | fix(acceptance): gate calibrated review rate | `c9c4b5c260f718eb8252fe50b27bbe92b3baed30` | 非 draft;MERGEABLE/CLEAN;CI backend+frontend 绿;仅 2 个文本文件,无二进制/秘密 |
| #40 | feat(review): scale manual decisions to filtered bulk operations | `0dea99256c6829de86610ef7cbac1b92f8a78ebf` | #37 合并后重判 CLEAN;CI 绿(head 293fc71);与 #37 文件零重叠 |
| #41 | feat(review-ui): optimize STEP3 for large-scale manual matching | `44bb8a8f7a9f2bf15414d365ba806adf466656ac` | #40 合并后重判 CLEAN;CI 绿(head e5a9afe);仅 web/ 范围 |

- **PR #38 未合并**(有意保持 OPEN):E2E 证据待本轮与 GPT5 部署专项全部集成冻结后重跑。
- 最终 main commit:**`af5507f`**(chore: 版本 1.1.5);**最终版本 1.1.5**。
- 合并后 main 独立验证:本地 pytest **137 passed**;`web npm run build`(browser-guard + 新增 `test:review-contract` 契约门禁 + vue-tsc + vite)全部通过。

## 2. 部署前基线(升级前实测)

- 部署 commit:`649fb4a`(版本 1.1.4,对应 #35/#36 合并后 chore);`current→releases/1.1.4`,`previous→releases/1.1.3`
- 服务:`material_matcher.service` active(uvicorn 127.0.0.1:18160)+ `frpc-material-matcher.service` active;基础 frpc 未触碰
- `MATERIAL_MATCHER_DATA_DIR=/var/lib/material_matcher`(软链 → `/oracle/material_matcher_data`,924G XFS)
- metadata DB:`/var/lib/material_matcher/meta/material_matcher.db`(1.8 GB,WAL)
- 健康:`/api/health {"status":"ok","version":"1.1.4"}`;`/api/health/ready` 四项 true;`https://mat.bjlzc.cn/api/health` 正常
- 当前 Python 环境:`/opt/material_matcher/current/runtime`(自包含 py3.11.16);前端:`current/web/dist`

## 3. 备份(升级前,均在仓外 `/oracle/codex/work/MATERIAL/deploy-1.1.5-20260917/backup/`,不入 Git)

- `material_matcher.db.pre-1.1.5` — sqlite3 online backup API,`PRAGMA integrity_check=ok`
- `config-etc-material_matcher.pre-1.1.5.tar.gz` — /etc/material_matcher 全量(含 secret,0600)
- 版本/commit 记录 + 部署前 DB 全表清点(见下"数据保留终检")

## 4. 升级方式(复用仓库既有交付链,未新发明流程)

1. `scripts/build_release.py`(复用既有 runtime/wheelhouse/model 输入;1.1.4→1.1.5 依赖零变化,仅版本字段)→ release-115
2. `scripts/build_offline_bundle.py` → `material-matcher-1.1.5-x86_64`(10,826 文件,内置校验 ok)
   - offline-manifest SHA-256:`47ee9daf9346721872300084f07208f6e191a1d50149095f646bdc1d5509ca21`(登记于 `release-build/bundle-115.sha`)
3. `verify_offline_bundle.py <bundle>` 正式校验(本机 x86_64,**无 --skip-arch**)通过
4. `./install.sh`(`MATERIAL_MATCHER_ALLOW_UNSUPPORTED_OS=1`,与既有内部环境一致):bundle 校验 → 版本化复制 → **旧服务在线时对新版本执行严格 doctor** → 停旧 → 原子切换 current → systemd 启动 → `/api/health/ready` readiness(失败自动回滚)
   - 插曲:SLES 系统 python3 无法解析校验脚本 f-string,在**预校验阶段即安全终止、零副作用**;按既有做法使用 `release-build/shim/python3` 后一次成功。**未发生 rollback。**

升级后:`current→releases/1.1.5`,`previous→releases/1.1.4`(回滚点保留)。

## 5. 升级后验证(smoke,全部通过;脚本与日志存于仓外部署目录)

基础健康:
- `/api/health version=1.1.5`;ready 4/4;`https://mat.bjlzc.cn/api/health` 经 nginx+FRP 隧道返回 1.1.5;index/assets 200;登录 + session 刷新保持;已有任务全部仍可打开
- 部署版 `material-matcher doctor --require-frontend --require-embedding --require-release-manifest` → ok=true(旧服务切换前后各一次)

STEP1/STEP2:
- 既有 10 万行任务 `SCALE-100Kx1M-C` 打开、summary 统计正常;双 Excel 工作流:新建 SMOKE 任务用 **xlsx 源 + xlsx 目录**上传→READY inspection→catalog 版本→草稿(dry-run)→正式任务 COMPLETED(1000 行)

STEP3(重点真实验证):
- 五状态服务端筛选(ALL/MATCHED/REVIEW/CONFIRMED/UNMATCHED)、50/100/200 分页、q 搜索、include_candidates=5 Top5 内嵌
- **server-side filter → selection-count(N=711/80,298 实测)→ 选择当前筛选全部 → `/workbench/bulk`**:请求体实测 **169 字节,不含任何行 ID**;既有 10 万任务 selection-count(MATCHED)=80,298 同样零 ID 传输
- 批量:确认第一候选(711/711 单事务 success)、标记均不匹配、取消人工匹配(显式 3 条带 version)、取消未匹配、恢复算法原始(713 filter);显式缺 version → 422 EXPECTED_VERSION_REQUIRED;伪造 version → **VERSION_CONFLICT**(bulk 与单行 decisions 均验证)
- 逐行 Top1～Top5 不同候选 decisions + REMATCH + unmatched,审计 operations 可查
- 双阈值 re-decide:**preview 模式不改数据**,apply 生效(835→711),人工已确认行不被重判覆盖;calibration 返回 top1_score_histogram + top1_top2_gap_histogram
- 人工匹配 Excel:下载(720KB 模板)→ 上传:成功应用 2 / 空白 997 跳过 / 同文件重复不同选择 **DUPLICATE_ROW_CONFLICT 不落库** / 与既有手工结果不同 **MANUAL_RESULT_CONFLICT 不覆盖** / 相同选择幂等跳过
- reviewer 角色对 `/workbench/bulk` 放行(临时账号实测 200,验证后即停用;既有用户与口令未动)

验收(#37):
- 金标评测(显式 NO_MATCH 新格式)产出 `review_rate=0.841` 与 `current_review_rate=0.714` 双口径
- 部署版判别探针(临时阈值 env,只读 DB,未改 /etc):上限 0.75 → gate=**PASS** 且 `effective_review_rate=0.714`(旧口径 0.841 会 FAIL)——证明按调参后当前口径把关;缺 review rate → missing_metrics BLOCKED(单测同口径)
- `material-matcher acceptance`:9 门禁 **5 PASS / 4 BLOCKED / 0 FAIL**,code_ready=true;BLOCKED 均为既有外部证据项(项目阈值未批、真实百万金标、麒麟实机、规模),与 9-15 上线报告一致,**非本轮回归**

STEP4:
- 既有任务 exports/结果 Excel 下载 16.2MB;SMOKE finalize→result revision→结果 xlsx 1.58MB;无回归

## 6. 数据保留终检(备份 DB vs 升级后 live DB,程序化对比)

- 32 个历史任务全部存在;10 万行任务 match_items 逐项计数不变
- reviews/audit/evaluation_runs/match_items/files/catalogs 等所有表**只增不减**(增量均为本轮 SMOKE 测试数据);未删任何数据、未重建 SQLite、未清空任务、未重置管理员密码、未 revoke 任何既有会话(唯一 revoke 对象是新建的一次性 reviewer,随后已停用)

## 7. 结论与遗留

- 升级成功;冒烟 56 项断言中 2 项为脚本自身测试数据构造缺陷(重复行值与原行相同,被正确判为幂等跳过),已用定向复测证实产品行为正确(见 smoke-checklist.md 与部署目录 excel-*-retest)
- **未跑最终 1000 行正式验收**(按指令留待 #37/#40/#41 + GPT5 部署专项集成冻结后重跑;#38 未合并)
- 遗留:
  1. vault 中 accounts.env 四账号口令与线上不一致(admin 实际沿用安装 bootstrap 口令)——属凭据簿维护问题,本 Agent 未改动任何口令;建议管理员线下核实更新(不阻塞)
  2. `/etc/material_matcher/acceptance.env` 项目批准阈值仍缺(生产验收 BLOCKED 项之一,需业务签字,不自行写入)
  3. 冒烟任务 `SMOKE-1.1.5 万级批量人工匹配验证` 及 1 个新 catalog/supplement 文件保留为测试历史(未清理,符合"测试历史全部保留")
- 本证据 PR **不包含**运行日志、密码、session、客户 Excel 或敏感配置;完整日志/备份在运维主机 `/oracle/codex/work/MATERIAL/deploy-1.1.5-20260917/`
