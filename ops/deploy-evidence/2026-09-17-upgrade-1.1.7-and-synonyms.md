# 部署证据 — 1.1.7 升级(#46 业务方案名统一)+ 同义词配置启用

日期:2026-09-17(UTC+8)。性质:升级(承接 1.1.6 报告);同义词维护为独立数据配置操作。

## 1. #46 合并后检查(由协作方 squash 合并)

- merge commit `dbf3f20d079508dbd70a8282e8013a22d466ba2f`,main → `dbf3f20`;7 文件,范围 STEP2-4 名称显示 + task_service 冻结 `scheme_display_name` + 新测试 121 行,无越界。
- CI run(backend+frontend)全绿;合并后本地 `pytest -q` **142 passed**;`npm run build`(browser-guard + STEP3 契约门禁)通过。
- 版本 chore → **`724786b`(main 最终,版本 1.1.7)**。

## 2. 升级

- 备份(pre-1.1.7):DB online backup(integrity ok)+ /etc 配置 tar + 基线清点(tasks=34, dictionaries=0 等),仓外 `deploy-1.1.7-20260917/backup/`。
- 交付链:build_release → bundle 1.1.6 复用 runtime/wheelhouse/model;manifest SHA `e91ae50aea845175c2b2bf03763d6e428dcc344c9421c69cdd23ca4baac03c61`(`bundle-117.sha`)。**过程纠错**:首次 build 误在仓库根目录执行导致 bundle 混入 1.1.6 旧 dist,已删除重建并逐文件比对确认新 dist(`index-BReg9MVh.js` 与 `web/dist` 一致、含 #46 的 `.task-name-row` 守卫)后才安装。
- `install.sh`(在线 doctor → 原子切换 → readiness)一次成功,未回滚。
- 结果:`current→1.1.7`,`previous→1.1.6`;公网 `https://mat.bjlzc.cn/api/health` → 1.1.7。

## 3. 升级后回归(#46 相关 + 关键链路)

- 新任务启动即在 config_snapshot 冻结 `advanced.scheme_display_name`(无模板方案任务显示友好回退"未命名方案" ✓;历史任务快照无该键 → 走视图回退,不受影响)。
- STEP3 五状态/分页、`selection-count(filter)`、bulk 冲突路径、人工 Excel 下载、Top1 与分差直方图、10 万行任务结果 Excel(16.2MB)— 全部无回归。
- 数据保留:pre-1.1.7 备份 vs 线上,所有历史表只增不减。

## 4. 同义词配置(独立任务,20 条,已启动并实测)

- **数据驱动选型**:从 1000 对金标配对中挖掘真实变体 — `试验所→实验所`(210 对)、`环氧酚醛玻璃布→环氧玻璃布`(150 对)、`三极管→晶体管`(70 对)、`层压布棒→布棒`(68 对)、`层压玻璃布棒→玻璃布棒`(67 对)等;其余为行业通用缩写(`运放→运算放大器`、`光耦→光电耦合器`、`贴片→表面贴装`、`塑封→塑料封装`、`接插件→连接器`、`∅→φ` 等),全部非嵌套(引擎按键长降序替换,`场效应三极管` 优先于 `三极管`)。
- **落地**:业务字典「物料同义词表(名称/厂家)」`c9b8a92a48374e1b96b14ac7bddb956a` v1(20 条 mapping,case_sensitive);通过 `dictionary_map`(mode=replace, on_missing=keep)挂接 6 个已发布分类方案(A001/A002/A003/A005/A006/A007)的**名称/分类描述/生产厂家**规则,源/目标两侧对称;**型号/规格/标准号等字母数字规则不挂接**,避免误伤。6 个方案均发布 v2(validate→publish,映射冻结进版本,历史任务快照不受影响,方案可 rollback 到 v1)。
- **A/B 实测**(7 行真实语料小任务,基线 vs 启用同义词):
  | 行 | 基线 | 启用后 | Δ |
  | --- | --- | --- | --- |
  | 三极管→晶体管 | 73.4483 | 83.1034 | +9.66 |
  | 层压布棒→布棒 | 86.0000 | **100.0000** | +14.00 |
  | 环氧酚醛玻璃布 | 92.2222 | **100.0000** | +7.78 |
  | 试验所→实验所 | 97.2727 | 100.0000 | +2.73 |
  | 3 个对照行(名称已一致) | 100.0000 | 100.0000 | 0 |
  - 提升 4 / 持平 3 / **劣化 0**,Top1 集团码指向全部不变;其中两行由"低于自动匹配阈值(88)进人工"直接升为 100 分自动匹配 — 定向增强达成。
- 证据文件:`logs/synonyms-setup.json`、`logs/synonyms-ab-test.json`(本地留存;本仓内数字为合成测试语料,非客户数据)。

## 5. 回滚点与遗留

- 代码:`previous→releases/1.1.6`;bundle 1.1.7 SHA 见 §2。数据:pre-1.1.7 备份。同义词回滚:6 个方案 rollback 至 v1(不含 dictionary_map),字典本身保留。
- 遗留:同义词表按真实客户目录回流后可继续以新字典版本扩充(任务快照冻结机制保证历史任务不受后续字典变更影响);vault 凭据簿更正与 acceptance.env 项目阈值两项不变;#42/#45 证据 PR 保持 OPEN 由维护者决定处置;#43(Kylin 部署专项)仍处于 CONFLICTING,待其作者 rebase 后另行审查,本轮未合并。
