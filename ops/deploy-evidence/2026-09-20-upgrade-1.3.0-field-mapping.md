# 部署证据 — 1.3.0「字段映射简化 + Column Profile + 人工 value_mapping + 单自动阈值」上线

日期：2026-09-20。执行人：admin。环境：mat.bjlzc.cn（源码/release 目录模式，install.sh 增量升级）。

## 基线（严格排除 #104）

- 上线功能基线：**`1fdfcb174b81eab2dd0337a6bdec985ecbcc97c3`**（#100 匹配方式简化 / #101 Column Profile / #102 value_mapping / #103 单自动阈值，均已在 main）
- **#104 保持 OPEN，未合并、未入部署**（main 此后仅追加 #105 版本源 bump `de34fa2`，两处版本字符串，无业务代码）
- 上线前生产：1.2.9（commit e0a4d4d）→ 上线后：**1.3.0**（release git_commit=1fdfcb1）
- 增量面：33 文件（+1394/−148），依赖零变化（pyproject 无改动、package.json 仅 scripts 追加）→ Python/Node runtime、wheelhouse、模型、Docker、浏览器全部复用，未重新下载

## 备份与回滚

- `/opt/material_matcher/backup-before-field-mapping-20260920/`：db-pre-1.3.0.db（integrity ok）、server.env、storage.env、BUILD_INFO、releases 链接快照
- 回滚：`previous→releases/1.2.9`；`bash installer/install.sh`（1.2.9 介质）或 current/previous 换链即可

## 部署

- `MATERIAL_MATCHER_ALLOW_UNSUPPORTED_OS=1 bash install.sh`（既有正式逃生参数）rc=0
- 升级安装：端口 18160 保留、`password_source=existing`、种子幂等（新增 0 / 跳过 6 + 1 词表）、uploads/results/models 未动
- 前端重新构建：dist `index-DPU3Zyrc.js`；后端 systemd 重启（升级流程内置）；health `{"status":"ok","version":"1.3.0"}`
- 数据计数不变：tasks=26→27（+1 为 e2e 验证任务）、profiles 含 6 正式方案、match_items 4048

## 五场景现场冒烟（API + DB 实证，admin 用临时账号 mm-e2e-tmp，验毕已删）

- **A 新方案**：双模板上传 → `/api/files/{id}/column-profiles` 客户侧枚举候选 {10,11}、集团侧 {国产,进口}，**服务端无任何自动配对键**；人工 value_mapping {10→国产,11→进口} + 匹配方式=精确（exact）→ validate/publish 成功，重进方案 value_mapping 仍在
- **B 旧方案兼容**：A001（无 Column Profile）草稿读取、原样重新发布 → 版本 5→6，历史版本全部保留；6 个旧任务仍冻结引用旧版本号
- **C 未配置值不猜测**：同方案任务实跑（源含 10/11/12 三行）：M001(10)→G001 100.0 MATCHED；M002(11)→G002 100.0 MATCHED；**M003(12) top1 仅 70.0 → REVIEW，未自动落码，原值 12 保留**
- **D 匹配方式**：新 dist 中「模糊相似/综合(字符+语义)/语义相似」字样 0 次；业务仅 精确匹配/智能匹配；A001 旧文档 contains/fuzzy/numeric 原样可读可发布（未偷改）
- **E 阈值**：dist 中「人工匹配下限/人工确认下限/复核下限」0 次，仅「自动匹配阈值」1 处；历史 review_threshold 列/数据未删未改
- 清理：E2E 方案已删、临时用户与 session 已删、临时口令文件删除；/api/profiles 401 正常鉴权

## 与安装介质

- mm-1.3.0 介质同源重建：tar `mm-1.3.0-x86_64.tar.gz`，1,238,536,963 B，SHA256 `3dcfc84d415bd7dec299267632f71cec160eecf655285ba89789966f3355752d`，镜像 `material-matcher-app:1.3.0`
- 麒麟客户机（mm-customer）一键重跑自动升级 1.2.9→1.3.0：rc=0，密码 hash `6d224127` 不变、6 方案 1 词表不变、镜像保留 1.2.9/1.2.8 回滚链
- 洁净室演练机 1.2.9→1.3.0 升级：rc=0，SMOKE 20/20，报告/日志落数据盘
