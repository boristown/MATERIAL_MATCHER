# 部署证据 — 1.1.23「原始输入冻结(#64)+ 时间语义统一(#65)」上线与验收

日期:2026-09-18(UTC+8)。执行人:admin。环境:mat.bjlzc.cn。

## 1. PR 与版本

- #64 `feat: 冻结任务原始输入并完善 STEP4 过程复原` — review 通过后 merge **`8f2735f`**(backend 186 tests + 前端契约本地全绿后才合并)
- #65 `fix: unify task start time and automatic compute duration` — 同步 main 后解 3 处冲突:
  1. `web/package.json` build 链:合并双方新增契约(`test:task-time-contract` + `test:task-input-assets-contract` 共存)
  2. `TaskWorkspaceBase.vue` STEP4:保留 #64「任务资料/本次使用的原始文件」结构,任务开始时间改用 #65 的 `formatTimePoint(started_at)` 并新增「自动计算耗时」列(:column=3)
  3. `result_export_service.py` import 合并(assets service + 时间格式化函数)
  - 同步中曾误把 node_modules 符号链接纳入 merge commit → CI 的 check_repo_files 拦截,amend 剔除后 backend/frontend 转绿(合并态本地 190 tests + build 全过)
- #65 merge **`8d7724e`**;版本 bump → **1.1.23** = `d0cbb00`
- 部署前 main=1.1.22、线上=1.1.15(并行 UX 版);bundle 复用缓存(runtime/wheelhouse/model/bootstrap-1116/node-offline-1116 未变),release/manifest/tar 全新构建,verify_offline_bundle ok(16167 files,无 skip-arch)

## 2. 部署安全

- pre-1.1.23 在线备份:integrity ok;tasks=24/files=65/catalog_versions=22;配置 tar;仓外 `deploy-1.1.23-20260918/backup/`
- install.sh(新版含 bootstrap 与介质完整性校验)一次成功:`current→1.1.23`,`previous→1.1.15`;未清库、未切 storage.env、未删历史任务(验收后 tasks 26、files 72,只增不减)
- 自动迁移在既有库上完成:`task_input_assets`、`task_compute_lifecycle` 新表创建;22 个历史 COMPLETED 任务冻结旧自动窗口;全部任务 `started_at` 回填为业务点击时间(=created_at)

## 3. 线上真实验收(admin 留痕,20/20 通过)

验证方案「原始输入与耗时语义验证方案」两次运行(第1次/第2次,均保留为测试记录):

### 原始输入(#64)
- 上传 3 行源清单 + 标准表 v1 精确 bytes → 启动即冻结;`/input-files/source|target` 下载 **SHA256 与上传字节完全一致**(`9fc92fa2…`/`fd1f42f5…` 等两轮回放一致),Content-Disposition 保留原始文件名(`验证源清单_20260918.xlsx`/`集团码标准_v1.xlsx`),FileResponse 原样字节(不重新导出)
- **target 不漂移**:向同目录上传并发布 v2(新集团码)并激活后,旧任务下载 target 仍为 v1 字节 ✓
- **历史任务(冻结表上线前创建)**:昨日验收任务经精确 `source_file_id`/`catalog_version_id→source_file_id` 关系恢复,下载 bytes 与 DB 记录 stored_path 内容 SHA256 一致 ✓(不按文件名猜测的负例由 CI 单测锁定)

### 时间语义(#65)
- `started_at` 与点击开始时刻 Δ=0.0s;自动计算窗口 `compute_started_at→compute_completed_at` 仅 84ms(scan 3 行)
- 人工等待 75s + re-decide(preview+apply)+ 人工标记未匹配 + finalize 生成结果后:`compute_duration_ms` 仍 84→84,窗口与 started_at 分毫未动;墙上时间跨度(~77s)与自动耗时严格分离
- STEP2 progress / 任务详情 / review-history / 工作簿四处暴露同一 `compute_duration_ms`
- 最终 Excel 摘要(4 列布局):任务开始时间 `10:33:42`、自动计算耗时 `0 秒`、自动计算完成时间 `10:33:42.582`、结果生成时间 `10:34:59.641` 各自独立、语义清晰
- 迁移后的旧任务:`started_at==created_at` 且冻结窗口可用(第1次运行 83ms)

## 4. 前端服务确认

- 公网 `https://mat.bjlzc.cn` index → `assets/index-K7JIHVeu.js`(新 dist,含「本次使用的原始文件」「自动计算耗时」等 STEP4/时间语义 UI)

## 5. 离线介质协调

- 已在 #62(最终离线包 clean-room 验收)留言:**1.1.22 不得作为最终冻结版本**;#64/#65 合入后从最新 main 重建介质,大型制品(runtime/model/wheelhouse/bootstrap)可复用缓存,但 release/manifest/SHA256/tar 必须重新生成,并补做 ①空环境 clean install ②1.1.13→1.1.23 既有库 upgrade 两项 migration 验证(本次 1.1.23 升级已提供后者在真实带数据环境通过的实证)

## 6. 回滚

- `readlink previous → 1.1.15`;install.sh --rollback 可用;备份链完整(未动数据目录)
