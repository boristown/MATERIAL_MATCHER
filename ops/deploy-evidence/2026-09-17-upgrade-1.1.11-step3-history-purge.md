# 部署证据 — 1.1.11「STEP3 历史计算记录列表」上线 + 历史数据清理

日期:2026-09-17(UTC+8)。执行人:admin(运维 Agent)。环境:mat.bjlzc.cn。

## 1. 部署

- PR **#50**(STEP3 历史列表替代任务下拉框)squash merge `eccb010`,CI 全绿;版本 `3a8d3c0` = 1.1.11。
- 构建链:build_release → build_offline_bundle(10,827 文件)→ 目标机 verify(无 --skip-arch)通过。
  offline-manifest SHA-256:`ce1e212ff2ed6f8d9e597fc86f07c8d34a64c31bdd67d43f24364e06125d869b`
- 安装前备份:`/oracle/codex/work/MATERIAL/deploy-1.1.11-20260917/backup/`(meta-pre-1.1.11、meta-pre-purge、etc 配置)。
- install.sh 一次成功,`current→releases/1.1.11`,readiness/health ok;数据目录/账号/既有任务未动。
- 注:并行 Agent 随后部署 1.1.12/1.1.13(#51),其 current 链正常收敛,见 1.1.13 证据。

## 2. 生产数据清理(业务负责人现场指示)

指示:「没有任务名概念,只有方案名;未命名方案下、不归属任何方案的记录没有业务意义,移除。」

- 移除 **16 条** COMPLETED 历史计算:均无 profile 关联且冻结名缺失/为「未命名方案」,实为运维会话产物
  (RBAC 探针×5、SCALE-100K×3、SMOKE-1.1.5/1.1.6、PR34 验收、AB×2、归一化对照×2、探针·A005),
  含候选 1,541,870 行、match_items 305,374 行、reviews 16,553、操作日志 1,452、阈值修订 6、评估 run 14。
- 移除 **2 条**已取消的压测/UI 验证 FAILED 任务 + **18 个**无方案归属草稿(同口径)。
- 同步删除孤儿上传/结果文件 43 个(约 78MB),仅删无任何存活引用者。
- 全程单事务 + `PRAGMA foreign_keys=ON`;两次 TASKS_PURGED_UNSCOPED / 一次 DRAFTS_PURGED_UNSCOPED 审计事件已写入。
- 完整备份保留于本机 backup/(非仓库);明细清单 `ops/deploy-evidence/1.1.11/`。
- 保留:22 条演示·A00X / 真实数据验证 / 上线验证任务(均挂 6 个已发布方案)与全部方案、字典、目录。

## 3. 验证(真实浏览器 Chromium headless + API)

- `GET /api/tasks/review-history`:20→22 条,全部显示 6 方案名之一,「未命名方案」残留 **0**,序号稳定。
- 页面实测(截图留存本机 `/oracle/codex/work/MATERIAL/deploy-1.1.11-20260917/artifacts/`,按惯例不入库):
  - 默认进入最新计算,历史列表 8 条预览 + 「当前查看」行高亮(蓝条+浅蓝底+标签);
  - 点击「A006 复合材料 · 第 4 次/共 4 次 · 09-16 12:25:51→12:25:54」→ 当前查看卡片、统计、工作台同步切换,URL `?task=` 更新;
  - **刷新浏览器** → 仍停在第 4 次计算,高亮保持;
  - 「查看全部计算记录」展开 22 条;
  - 页面全文无 SMOKE-*/TASK-*/run-*/未命名方案;顶部历史 dropdown 已消失。
- API 聚合耗时 0.09s(2GB 元数据库、含 100K 行历史期数据)。

## 4. 结果

历史列表不再出现「未命名方案」;同方案多次计算以「第 N 次计算/共 M 次 + 开始/完成时间」唯一可辨。

## 5. 追加(2026-09-17 21:0x,负责人指示)

移除「上线验证·名称精简专项」「任务名称移除验证方案」两个验证方案及其对应记录:
2 个已完成任务(含 #51 名称对抗样本任务,RESULT 阶段,3 行)、2 个绑定草稿、2 条发布版本;
方案本体经正式 `DELETE /api/profiles/{id}`(200×2)删除;孤儿文件 4 个(31KB)同步清理。
删除前备份:`deploy-1.1.11-20260917/backup/meta-pre-purge-2/`;审计事件 TASKS_PURGED_UNSCOPED 已记录。
终态:方案库恰为 6 个 A00X;历史计算 20 条全部归属 6 方案;草稿 25 个全部挂方案;tasks=20。

## 6. 第四步「没有形成有效匹配结果」处置(负责人报障)

- 根因:清理后第四步按结果文件 created_at 取最新,命中的是 A007 演示运行(150/150 全未匹配),页面展示正确、无代码缺陷。
- 附带发现:对同一任务重复 finalize 会复用旧 file_id 且不刷新 files.created_at,导致第四步排序不前进(UX 隐患,建议后续 PR:finalize 生成新文件记录)。
- 处置:以真实业务链路重跑 A006 复合材料(11 行真实源数据)→ 计算 auto=2/待人工=4 → 人工按分差确认 3 条 top1、对 gap=0 平分歧义 1 条标记均不匹配 → finalize 新文件(21:21:25)。
- 终验(Chromium 截图 `step4-normal-result-final.png`,留存本机):第四步显示 A006 · 总数 11 / 自动 2 / 人工 3 / 未匹配 6 / 待处理 0,「需要关注」警示消失,预览含真实业务行。

## 7. 第四步展示运行(负责人要求:1000 行、≥90% 自动、含人工处理与同义词)

- 同义词字典升版 v3(35 条:新增 积层板→层压板、平垫片→平垫圈、模压棒→模制棒、DC-DC→DC/DC、螺帽→螺母、光耦合器→光电耦合器 等);A001 元器件方案发布 v5 绑定字典 v3。
- 以 origin 种子派生 1000 行验收数据集(833 完全样本 + 50 原目录外样本并入 + 17 同义词变体 + 50 型号扰动硬样本 + 50 真实不可匹配),阈值 92/人工带 76:
  **自动匹配 930(93%) · 人工匹配 20 · 未匹配 50(5%) · 待处理 0**,任务快照绑定字典 v3(名称/厂家两列 pipeline),
  finalize 结果文件 21:37 生成 → 第四步「最近一次已生成结果」即为该记录,截图 `step4-showcase-1000.png`(本机 artifacts)。
- 数据构建脚本与分组清单留存:`/oracle/codex/work/MATERIAL/showcase/`(build_showcase.py, groups.json, showcase_source_1000.csv, showcase_truth.csv)。
