# 部署证据 — 1.2.9「Composite 跨类目组合方案 + 双模板识别 + 固定值/源过滤」上线

日期：2026-09-20。执行人：admin。环境：mat.bjlzc.cn（本机 SLES 宿主，`MATERIAL_MATCHER_ALLOW_UNSUPPORTED_OS=1` 正式逃生参数，与 1.1.24 部署同法）。

## PR 与提交

- 业务合入：**#92**（组合方案与子方案版本冻结模型）、**#94**（Composite 多子方案独立匹配与候选汇总）、**#95**（多目标文件输入与完整追溯）、**#96**（A007 前端产品化）、**#97**（模板识别、固定值与源数据过滤）
- 介质与安装器增量：**#98**（release/media-1.2.9），main 冻结于 **`e0a4d4d`** 后合入为 **`2508dec`**
- 部署制品：`mm-1.2.9` 原生轨 `n/` 目录（release git_commit=`e0a4d4d…`；构建源码记录于结果 JSON）
- 版本 1.1.24 → **1.2.9**（`/api/health` 实测 `{"status":"ok","version":"1.2.9"}`）

## 数据保护与升级

- 升级前备份：`/var/lib/material_matcher/backups/manual-pre-1.2.9-20260920.db`，`PRAGMA integrity_check` = ok
- install.sh 升级一次成功：rc=0；`mode=升级安装`；端口 **18160 保留**；`password_source=existing`（admin 密码未重置，users 表 admin=1）
- 种子幂等实跑：`新增方案 0 个，跳过已有 6 个；同义词表新增 0 张，跳过已有 1 张`（未覆盖生产任何既有配置）
- 前向迁移（无 destructive SQL、未清库）：`match_candidates` 新增 `child_profile_id/child_profile_version/child_profile_name/target_file_id/target_file_name`；新表 `task_draft_targets`；`task_input_assets` 保留
- 升级后计数不变：tasks=26、profiles=7、files=72、match_items=4048、uploads 目录 50、results 目录 22
- 回滚点：`current→releases/1.2.9`，`previous→releases/1.1.24`

## 访问验证

- 本机 `http://127.0.0.1:18160/api/health` 200；`/` 200
- 公网 `https://mat.bjlzc.cn/` 200，前端新 dist `index-CfG58va9.js`（含双模板识别/固定值/源过滤/A007 组合 UI）

## 与离线介质的关系

- 本次线上部署与交付介质共用同一 release 制品（release-129，`e0a4d4d`+介质增量）；Docker 轨全新演练 SMOKE 22/22，Native 轨全新演练 SMOKE 22/22（详见 test-results/final-installer 记录）
