# 部署证据 — 1.1.6 升级(#44 STEP3 单阈值调参与 Top5 候选详情)

日期:2026-09-17(UTC+8)。性质:升级(承接 1.1.5 报告)。

## 合并

| PR | 标题 | merge commit |
| --- | --- | --- |
| #44 | feat(step3): 单阈值调参与 Top5 候选详情 UX 精修 | `f28a711613939a9b92eb0cbbe1b8b75deb88739b` |

- 合并前:非 draft、基于 main `af5507f` 时 mergeState=CLEAN、CI run #291(backend+frontend)全绿;7 文件范围与申报一致(后端 2 + 测试 1、前端 4),未触碰 matcher/召回/评分/安装器/STEP4。
- 最终 main:**`5a88a83`(版本 1.1.6)**;合并后本地 pytest **138 passed**、`npm run build` 含新契约门禁(`STEP3_SINGLE_THRESHOLD_REVIEW_FLOOR`、禁"人工处理下限/双阈值"字样、禁写死 Z001/Z006)通过。

## 升级

- 备份(pre-1.1.6,仓外):DB online backup(integrity ok)+ /etc 配置 tar;版本记录 current→1.1.5 / previous→1.1.4 / bundle-115 SHA。
- 链:build_release → bundle 1.1.6(10,826 文件,`verify_offline_bundle` 无 skip-arch 通过,manifest SHA `dbdd328254c3d7cc203a15d633257715c1c6b2bcf05fde69293d6984a7edb774`,登记 `release-build/bundle-116.sha`)→ `install.sh`(ALLOW_UNSUPPORTED_OS=1,在线 doctor→原子切换→readiness)。一次成功,未回滚。
- 结果:`current→releases/1.1.6`,`previous→releases/1.1.5`;端口/数据目录/管理员口令文件 mtime 未变;公网 `https://mat.bjlzc.cn/api/health` → 1.1.6。

## 升级后验证(真实部署实例)

- STEP1~4 回归:10 万行任务打开/统计/结果 Excel(16.2MB)正常;SMOKE-1.1.6 双 xlsx 上传→任务 COMPLETED;五状态/分页/搜索、`selection-count`+filter 全选 bulk(169B 请求体,零 ID)、VERSION_CONFLICT、Excel 下载、直方图接口、200 页上限 — 全通过。
- **#44 单阈值语义实测**(SMOKE 任务,精确行级断言):
  - preview 带 `single_threshold=true` 且 `review_threshold=0.0`;preview 不改数据;legacy 默认 `single_threshold=false` 行为不变
  - apply 后:人工 CONFIRMED 行、人工 UNMATCHED 行均未被重判覆盖;低于阈值的正分候选行进入 REVIEW 交人工
  - **inclusive 边界判别**:对存储分恰为 90.5405 的行,同一请求参数下 legacy `>` → matched=0,`single >=` → matched=40,边界行状态 MATCHED ✓
  - 注:数据集中最高分 73 行(92.8571)全部命中既有的 **Top2 同分歧义保护**(rank-2 不同集团码且分差<1e-6 → 不自动匹配),属设计内守卫,非边界缺陷;无候选/0 分行为合成数据集不存在该形态,由 #44 单测 `no-candidate` 场景锁定
- 验收门禁(部署版 CLI):5 PASS / 4 BLOCKED / 0 FAIL,code_ready=true,与 1.1.5 一致(外部证据项未变)。
- 数据保留终检(pre-1.1.6 备份 vs 线上):33 个历史任务全部存在,`SCALE-100Kx1M-C` 10 万行不变,各表只增不减(增量=SMOKE-1.1.6 冒烟数据)。

## 回滚点

previous→releases/1.1.5;bundle 1.1.5(§前报告)+ 1.1.6 备份 DB/配置。

## 遗留

- 与 1.1.5 报告相同:vault 凭据簿需线下更正;acceptance.env 项目阈值仍缺;冒烟任务 `SMOKE-1.1.6 单阈值验证` 保留为测试历史。
- 人工建议的浏览器滚动手感验收(展开大量字段/小窗口)仍需 UI 侧肉眼确认,不在 API 证据范围。
