# 部署证据 — 1.1.24「#67 STEP4 业务指标与结果区 UX」上线

日期:2026-09-18。执行人:admin。环境:mat.bjlzc.cn。

## PR 与提交

- **#67**(feat/step4-business-result-ux-20260918)review 通过(纯前端 5 文件;formatter 纯函数契约实跑 93.0%/2.0%/5.0%/0%/—/0.2%;CSS stacked bar 带 aria+文字 legend;evaluation 路由保留、普通结果页入口移除)→ merge **`c91eee7`**
- 收尾 **`b79b766`**:向导 STEP4(TaskWorkspaceBase)同步移除「准确率验收」按钮、统计条「人工确认→人工匹配」、提示语对齐真实工作表名「人工操作记录」;`test-step4-result-contract.mjs` 扩展锁定向导 STEP4;版本 bump **1.1.24**
- STEP2(TasksView)的准确率验收入口按 #67 范围保留(正式验收能力需入口)

## 验证

- 合并态本地:`vue-tsc -b` + 全部 11 项前端契约(含新 step4-result + task-time + task-input-assets + business-name)+ backend 190 passed
- bundle 1.1.24:`verify_offline_bundle` ok(16170 files,无 skip),SHA `31315ce3…`;release/manifest/tar 自 main@b79b766 全新构建,dist 由该 commit 重新打包,bootstrap/node-offline/runtime/wheelhouse/model 复用缓存(未变)
- install.sh 一次成功:`current→1.1.24`,`previous→1.1.23`;pre-1.1.24 备份 integrity ok(tasks=26/files=72),未清库/未切 storage.env/未删任务
- 公网 dist `index-CDPM9GXP.js` 含「自动匹配率/人工匹配率/未匹配率/匹配结果构成/不代表匹配结果是否正确」;「准确率验收」字样仅存在于 TaskEvaluationView 与 STEP2 入口(合规),STEP4 两处已消失
- 非回归:#64 原始输入 SHA 不变(`9fc92fa2…`/`fd1f42f5…`);#65 started_at=10:33:42、compute_duration_ms=84 固定;已 finalize 的运行(第2次)结果 Excel 200/10,571 B;未 finalize 的运行正确 409

## 离线介质协调(已通知)

- #62 两次留言:冻结基线顺延为 **main@b79b766 / 1.1.24**(1.1.22、1.1.23 均作废);要求从该 commit 重建前端 dist 与 release/manifest/SHA/tar,并补做 clean install + 带数据 upgrade 两项 migration 验证

## 回滚

- `previous→1.1.23` + pre-1.1.24 备份;install.sh --rollback 可用
