# 1.3.21 现场验收清单（13 所）
1. 版本三连：/api/health=1.3.21；页面左下 v1.3.21；登录页脚注一致。
2. 上传 90 万行目标文件：浏览器不再假死，几十秒内返回（徽章显示行数带"约"为估算）；期间刷新/点其它页面均正常。
3. 组合任务"开始匹配"：秒级进入进度页，任务显示"冻结输入中"→约 1 分钟内"排队中"；不再出现 20 分钟草稿。
4. 另一任务满载时点"实时看板"：直达进度页（不再落回上传页）。
5. STEP3"生成最终结果"：几秒返回"后台生成中"，可离开；STEP4 未就绪时提示稍候而非转圈；就绪后下载成功、无误报。
6. 打开结果 Excel：匹配结果区在最左（含目标行号）；映射字段成对相邻并四色标注；右侧完整数据专区不置灰；4 万行级文件秒开。
7. 值映射：连续填多个映射不弹关；权重调高后 STEP3 卡片预览字段跟随。
8. 人工匹配工作簿导出：映射字段列在前（按权重），导入回归正常。
9. 数据保真抽查：升级前后任务数/方案数一致（select count(*) from tasks/profiles）。
10. 回滚演练（可选维护窗口）：./rollback.sh → 1.3.20 健康检查通过 → 再 ./upgrade.sh 恢复。
