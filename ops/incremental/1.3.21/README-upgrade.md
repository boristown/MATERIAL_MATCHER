# MATERIAL_MATCHER 增量升级包 1.3.20 → 1.3.21（13 所现场问题批修复）
一次带回九项：#184 定稿导出后台化、#185 上传不假死、#186 开始匹配秒回（冻结输入中）、#187 下载误报消除、#188 大文件解析提速+估算、#189 结果 Excel 成对版式+xlsxwriter 引擎、#190 值映射弹层连续录入、#191 实时看板直达、#192 卡片/工作簿按权重排序。

- 用法（root，宿主机）：`unzip/sha256sum -c → tar -xzf 或 unzip 本包 → ./upgrade.sh`（自动识别 Docker/Native；`--docker 容器名`/`--native` 可显式）
- 预检强制：当前版本必须 = 1.3.20；包体 SHA 校验；已装则 PRECHECK-SKIP 幂等退出。
- **本版新增离线依赖 xlsxwriter（纯 Python，无二进制）**：升级脚本自动从包内 vendor/ 离线安装到运行环境（不联网）；安装失败会在替换应用前中止，不留半更新。
- 自动备份到 `/var/lib/material_matcher/backups/incremental-1.3.21-<时间戳>/`：应用、前端 dist、数据库（sqlite 在线备份）、上传清单、/etc 配置。数据库无任何破坏性操作；回滚 `./rollback.sh`。
- 完成判据：`/api/health` 含 `"version":"1.3.21"`；页面左下角 v1.3.21；开始匹配立即出现"冻结输入中→排队中"；生成最终结果几秒内返回"后台生成中"，稍后在第四步直接下载。
- 行为变化速览：STEP4 下载按钮在结果生成中会提示"稍候"（不再无限转圈）；值映射弹层连续录入不闪关；结果 Excel 新列序（匹配结果区|映射依据成对四色|完整数据专区）。
