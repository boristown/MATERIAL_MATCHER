# 13 所现场部署日志 · 2026-09-24（升级日）
目标：10.96.60.114（当前 1.3.15 + patch1 + 手敲草稿过滤）→ **1.3.20 + r1 热修**
纪律：每步登记（命令/输出摘要/结论）；任何一步异常即停，远程确认后再继续。

## 步骤记录
- [x] **S1 物料就位** 全部补丁与介质打包为单个压缩文件，已拷至服务器：`/usr/mm1.3.20_20260924.zip`（含 1.3.20 全量介质/增量包/field-patch-r1/说明）
- [x] S2 解包并核对 ✅ /usr/mm1320 清单：field-patch-r1.sh、MATERIAL_MATCHER-incremental-1.3.16-to-1.3.20.zip、mm-1.3.20-x86_64.tar.gz、两个 .sha256（齐全）
- [x] S3 升级前快照 ✅ 现场实测：profiles=6 / dictionaries=1 / tasks=8 / 应用版本=**1.3.16**（增量包端到端验证过的起点）
- [x] S4 增量升级 ✅ 1.3.16→1.3.20 `UPGRADE-SUCCESS`（预检 OK/PAYLOAD-SHA OK/自动备份/健康三重校验通过）
- [ ] S5 执行 `bash field-patch-r1.sh`（STEP4 未定稿可导出）
- [ ] S6 验证：三源版本一致 / 页面左下角 v1.3.20 / 历史任务与方案完好
- [ ] S7 功能抽测：真实数据跑一单（阈值 50，看自动匹配量）+ 未定稿直接点下载应出 Excel
- [ ] S8 遗留处理：A002~A006 假列名行（若未删）→ 界面删或 unseed；结果 Excel 版式优化后续版本
- [ ] S9 备份路径交代 + 回滚演示窗口（如客户同意）+ 交付签字页

## 现场命令区（逐步回填）

- S2 输出摘要：现场 unzip 成功（麒麟自带 unzip），目录 /usr/mm1320；下一步 S2b 校验 sha 并解开增量 zip。

- S4 完成时间：2026-09-24 现场；备份目录见现场回显（/var/lib/material_matcher/backups/incremental-1.3.20-*），待补抄路径。
- 进行中：S5 状态待回显确认；S7 首测=A003 金属材料 22 万目标行，ETA≈48min（串行建议已给：期间不叠加第二匹配）。r1 验收将在该单完成后以"未定稿直接点下载"验证。
- 负载判读：nproc=128，load1=127.26（满载爬升）→ 判定串行；r1 补丁含 restart，必须等 A003 完成后再执行（顺序：A003 完成→试下载验 409→打 r1→复测→第二单）。
- 并发调优方案（128核）：默认 ONNX 推理吃满全核导致单任务即满载；设 MATERIAL_MATCHER_EMBEDDING_INTRA_THREADS=42（写入 /etc/material_matcher/server.env 后重启容器）可实现 2~3 个大任务真并发；操作窗口=空闲时（重启打断在跑任务）。
- 现场缺陷#181（排队时点实时看板落回上传页）根因=启动后草稿残留；修复 main=31ce835（PR#182，CI绿，issue已关）；现场手敲版(r2-mini 11行)已发并测试床实测通过；全量脚本 field-patch-r2.sh 挂 Release v1.3.20。执行窗口=A003 完成后、与 r1 同批（都要重启）。
- 现场补丁操作规范（按现场指正修正）：停任务(update FAILED)→重启杀worker→查 PENDING/RUNNING/PREPARING/RECOVERING 必须为空（恢复机制会捞活任务成'野任务'）→再打 r2/r1→最终重启→复查明空→重跑。两个检查点 SQL 已发。- r2 定稿（NOT EXISTS 版，防误伤编辑中草稿）合入 main；四断言测试床全过；现场手敲 4 行版已交付。
- 新增问题记录：#184 finalize 万级耗时/无进度/第四步假死 —— 现场已答复临时口径，修复排 1.3.21。
- 新增问题记录：#184 finalize 万级耗时/无进度/第四步假死 —— 修复排 1.3.21，现场临时口径已发。
- 新增问题记录：#185 STEP1 大文件上传同步解析假死 —— 与 #184 同批修复（1.3.21），现场临时口径已发。
- 新增问题记录：#186 开始匹配同步 freeze_draft 全表解析致跳转延迟 —— 与 #184/#185 同批修复（1.3.21），临时口径：只点一次等待跳转。
- 现场瓶颈确认：#186(start冻结) + #184(finalize导出) 两处；今晚产出 r3 文件级热修（2 个后端 py 文件 + 重启，含回滚），完整异步化+进度条入 1.3.21。待现场提供源/目标行数复现量级。
- 现场确认：finalize(导出) 与 start(冻结) 经 SQLite 写锁互相卡死；已给观察/破锁流程；1.3.21 锁分离优先级提高。
- 20min 后：任务B 进入实时看板（start 解锁）；任务A finalize 转圈=导出与任务B 抢 CPU，判据=轮询 result_file_id；出现值即 F5 刷新下载，禁止二次点击。
- 新增问题记录：#187 下载误报请求失败（下载导航取消同页XHR+拦截器兜底误报）——1.3.21 修，P3 观感级。
- 新增问题记录：#188 组合上传 90万行 10+ 分钟无进度条——归入 1.3.21 性能包（calamine 解析+异步+进度+流式导出），与 #184-#187 同批。
- 新增问题记录：#189 结果Excel成对并排版式+一致性颜色+完整数据专区+xlsxwriter 高性能导出（接管并关闭 #165）；版式规格按现场原话落成验收标准。
- 新增问题记录：#190 值映射弹层填一格关一次（popover v-if 依赖编辑态；修复=受控visible或改小dialog连续录入），随 1.3.21 前端批。
- 新增问题记录：#192 实时看板落入上传页（组合恢复链 restoreCompositeTargets 未包 catch，忙时中断停在stage0）——1.3.21 修：先跳看板后补载。临时口径：F5/等完成点查看。
- 修正记录：实时看板落上传页单实际编号 #191（标题已改）；跨类目'排队显示为草稿'并入 #186 根因（start 同步冻结期无任务行）。当前现场未决清单：#184-#191 七单 + #162 formal。
- #186 追加现场实测：草稿态持续约20分钟→排队，用户判断'后台事被前台做'成立；已提级P1+验收基准锁定。
- 新增问题记录：#192 卡片字段取前N条规则而非权重降序 + 人工匹配工作簿全列平铺——1.3.21 修（权重排序+成对列序），打分本身不受影响。


## S5 · 当晚修复完成（2026-09-24 深夜，我方侧）
现场登记的全部问题已修复合入 main：#184 #185 #186 #187 #188 #189 #190 #191 #192（PR 193-198）。
性能实测（我方 128 核 Xeon 主机，同机放大现场 4 倍数据量）：
- 90 万行上传+建目录：61s+101s → 22s+0s；期间页面可正常操作（不再假死）
- 开始匹配：秒级返回（任务"冻结输入中"→"排队中"），不再 20 分钟草稿
- 生成最终结果：秒级返回"后台生成中"，可离开页面；4 万行×20 万候选导出 36 秒（新版式：映射字段成对+四色一致性+完整数据专区）
→ 1.3.21 增量包（1.3.20→1.3.21）+ 全量介质今日出，随下次进场/快递交付。


## S6 · 1.3.21 发布完成（2026-09-24 深夜）
- tag v1.3.21 + GitHub Release（含增量包 asset）；9 个现场 issue 全部关闭（#184-#192）。
- 增量包 SHA 前缀 973acc15：MATERIAL_MATCHER-incremental-1.3.20-to-1.3.21.zip（FROM 仅 1.3.20，预检硬门；vendor/ 自动离线装 xlsxwriter）。
- 双环境实测：我方线上 18160 与 客户测试床 material_matcher-app 均由 1.3.20 升级至 1.3.21，HEALTH+READY 通过，SMOKE 各 21/21；客户库数据保真（17 任务/7 方案，admin 口令哈希升级前后逐字节一致）。
- 13 所部署提示：宿主机解压后直接 `./upgrade.sh`（自动识别 Docker 模式并自动重启容器）；若手工在容器内执行 native 模式，收尾需宿主机 `docker restart material_matcher-app`。回滚 `./rollback.sh`。
- 待办：#162 formal（生产模型+客户数据 4万×100万 现场跑，验收清单第 8 条）；下次进场带 1.3.21 介质。
