# 物料集团码匹配引擎：开发实施强制规范

> 说明：本文件的既有第 21～40 章保持 v2.3 定义；v2.4 新增的 Embedding 模型与批量性能强制规范以 [`EMBEDDING_AND_PERFORMANCE_DESIGN.md`](./EMBEDDING_AND_PERFORMANCE_DESIGN.md) 为准，且与第 35 章单比特向量候选召回共同构成实现契约。

> **v2.4 强制补充：**默认生产 Embedding 为 `BAAI/bge-base-zh-v1.5`（768维），但必须通过 Provider 可替换；CPU-only 现场必须支持自动 Benchmark、预计完成时间、Target embedding 缓存复用、流式向量化、1-bit BBQ + int8 sidecar。禁止全量 float32 常驻/整体落盘后再二次量化。参考规模 100万 Target + 10万 Source 的首次全量产品目标为 2～4 小时，后续同目录任务 20～90 分钟。详细磁盘和内存预算见专项文档。

本文件的原有强制规范内容保持不变，以下章节定义仍按原文执行：

- 第 21 章：规范使用规则；
- 第 22 章：技术栈与工程约束；
- 第 23 章：推荐代码目录与模块职责；
- 第 24 章：核心业务对象定义；
- 第 25 章：元数据持久化设计；
- 第 26 章：文件上传与表格识别契约；
- 第 27 章：数据集读取与多表关联；
- 第 28 章：字段引用、组合与转换；
- 第 29 章：可配置数据标准化与字段处理引擎；
- 第 30 章：同义词词典；
- 第 31 章：字段匹配算法契约；
- 第 32 章：字段权重与综合分公式；
- 第 33 章：显示分与判定分；
- 第 34 章：分类/物料组范围规则；
- 第 35 章：单比特向量候选召回规范；
- 第 36 章：候选召回策略；
- 第 37 章：决策规则；
- 第 38 章：匹配任务状态机；
- 第 39 章：输出 Excel 精确定义；
- 第 40 章：匹配方案配置模型。

为了避免多份大文档之间再次产生重复和冲突，v2.4 不在本文件重复复制 Embedding 性能专项正文；开发实现必须同时满足本文件和专项文档。