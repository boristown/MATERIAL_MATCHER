# 人工断网离线部署记录（客户体验轮）· Docker 方式 · 介质 1.2.5

- 时间：2026-09-19（容器内时间 02:54 完成安装；证据采集 10:59）
- 环境：mm-customer（银河麒麟 V10 Lance，systemd，internal 网络**无外网出口**，无任何预装 Docker/项目组件）
- 接入：`ssh -p 17470 root@39.104.206.210`（frp 独立隧道 `frpc-mm-customer.service`；SSH 服务端为介质自带 bootstrap Python 运行的 paramiko `mm-sshd.service`，沙箱专用）
- 操作人：项目负责人（人工逐条敲命令，与真实客户一致）
- 结论：**PASS（一键安装成功）**，全程约 12 分钟（<30 分钟目标）

## 一、人工执行序列（转录已脱敏）

```text
cd /root && ls                                   # 仅见：客户交付介质
cd 客户交付介质 && ls
  MATERIAL_MATCHER-1.2.5-KylinV10-x86_64-双轨最终交付介质.tar.gz
  介质SHA256SUMS.txt
sha256sum -c 介质SHA256SUMS.txt
  → MATERIAL_MATCHER-...tar.gz: OK
cd /root
tar -xzf 客户交付介质/*.tar.gz                    # 一次误粘贴（文件名被截断）报错后重输，解包成功
ls → MATERIAL_MATCHER-最终离线交付介质-1.2.5-x86_64  客户交付介质
# （注：全量校验行被终端粘贴吞行未显示结果——由原厂在完全相同环境补跑：
#   sha256sum -c SHA256SUMS-整个交付介质.txt → 18636 个文件，0 不匹配，见 post-install-verification.log）
cd MATERIAL_MATCHER-最终离线交付介质-1.2.5-x86_64
cd 01-Docker方式 && ./启动Docker安装.sh
```

## 二、向导逐屏记录（屏幕原文摘录）

| 屏 | 屏幕内容 | 人工输入 |
|---|---|---|
| 欢迎/二选一说明 | 版本 1.2.5（x86_64），全程离线 | 自动继续 |
| 环境检查 | 8 项 ✅（OS/架构/systemd/自带运行环境/离线组件 27.1.1+Compose v2.29.7/镜像 tar/业务数据/磁盘 719GB）+ ℹ️"本机没有 Docker，将由介质离线安装" + ℹ️"首次安装" | 自动 |
| 服务端口 | 默认 18080 | 回车 |
| 管理员密码 | y=手工 n=自动 其它键=安全退出 | n |
| 确认安装 | 版本/端口/密码方式/Docker 组件/数据目录/离线声明逐项列出 | y |
| 安装进度 | 3%校验介质→6%Docker 环境→8%离线装引擎→14%Compose→20%导入镜像→55%持久目录→62%启动容器→66%等待就绪→90%导入 6 方案+同义词→96%报告→100% | — |
| 安装成功 | 版本 1.2.5；admin；密码文件与安装报告路径；"初始密码（请立即保存）：Z6**（已脱敏，仅交互终端显示）"；维护与 Win7 浏览器提示 | — |

进度阶段连续输出，**无 >30 秒无输出**；向导结束时刻 02:54:51，安装报告 `docker-install-20260919-025451.txt`。

## 三、装后取证（原厂于同一环境补做，10:59）

| 检查 | 结果 |
|---|---|
| 交付包 sha256 -c（人工已做） | OK |
| 全量逐文件校验（补跑） | 18636 文件，**0 不匹配** |
| docker.service | enabled（开机自启） |
| 容器 | material_matcher-app Up（image 1.2.5） |
| /api/health / /api/health/ready | ok 1.2.5 / ready |
| 介质自校验 verify_docker_bundle | ok=true（3082 files） |
| 原厂 smoke | **22/22**（6 方案名单、同义词版本链、批量删除模拟、A007 方案 STEP1→4、Excel 下载） |
| 浏览器路径（宿主机→内网IP） | GET / 200 |
| 环境重启（docker restart 等效） | 15 秒内容器自恢复，health 1.2.5 |

原始日志：本目录 post-install-verification.log、restart-autostart.log。

## 四、如实记录的观察项（不影响 PASS）

1. 人工一次误粘贴导致 `tar` 报错一次、全量校验结果行被吞——操作习惯问题；该 20 秒级校验由原厂补跑并记录（0 不匹配）。
2. 向导成功页在"安装器尾屏已列出 172.30.88.2:18080"的同时仍显示"未检测到局域网 IPv4"——成功页与尾屏的解析时序显示不一致（显示级瑕疵，信息已通过尾屏与报告正确送达）。已建 GitHub issue 跟踪，将随下一版本修复并重建介质。
3. 容器时钟为东八区，报告文件名时间戳与运维记录存在 8 小时时区差（环境特性）。
4. SSH 隧道/服务端为测试沙箱专用组件（介质外的运维工具），不参与、不影响客户机离线安装本身。

## 五、本轮人工验收判定

**Docker 方式一键断网离线安装：PASS（人工一次成功，全程 <15 分钟，无手册外操作，无卡死，介质三级完整性成立）。**
