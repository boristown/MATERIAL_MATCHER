# 1.3.12 内网部署风险复测（人工匹配 Excel 重点）

| 场景 | 结果 |
|---|---|
| 人工 Excel 填 候选N（Top5 全列真实存在，含低分候选）| 通过——"候选3"对应第 3 名真实候选，映射一致 |
| 乱填文本（"随便写"）| 拒绝：INVALID_MANUAL_SELECTION 精确到 Excel 行号 |
| "均不匹配" | 通过：行转 UNMATCHED，不计匹配 |
| 改工作表名上传 | 拒绝 422：未找到"人工匹配"工作表 |
| 定稿后再人工调整 | 后端 exports.stale=true；STEP4 主下载按钮警示+禁用（警示文案随 1.3.12 dist 发布）；结果历史修订表按版本各自可下载（天然防错）；重新生成后恢复（API rev1→rev2 实测） |
| GBK 编码 CSV 上传 | 解析正常（表头置信 1.0） |
| 上传去重 | 同内容改名两次 → 引用同一物理文件（1.3.10 起） |

新增修复：#136（exports stale 标记 + ResultsView 警示/禁用下载）。main=84e4b92。
介质：mm-1.3.12（1,349,874,417 B / SHA 97ea2645…），线上+客户机+洁净测试床三处 health 1.3.12。

## 遗留提示（不阻塞交付）
- 客户机房若启用 SELinux enforcing：如遇服务无法绑定端口，运行 `getenforce` 确认并联系系统管理员放行（介质 FAQ/ops.md 已含指引）；
- 服务器时钟漂移影响会话过期判断：部署后建议校时；
- 定稿后警示文案的 UI 端到端断言未做视觉回归（后端标志与按钮禁用逻辑已单测/构建覆盖），下轮补。
