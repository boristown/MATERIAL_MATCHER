# Test A/H 正式验收安装记录（Test A2）

- 执行人角色：未参与 MATERIAL_MATCHER 开发的运维安装员
- 唯一参考资料：`manual-copy-安装手册-v2.md`（介质 `docs/安装手册.md` 原样副本）
- 目标环境：docker 容器 `mm-cr-a2`（银河麒麟 V10，x86_64，完全离线，无浏览器/无图形桌面）
- 安装介质目录：`/root/物料集团码智能匹配平台安装包`（第 1 步已复制入容器）
- 严格禁令遵守：未读取仓库源码；未 `cat`/读取 `install.sh`、`install_wizard.sh`、`mmctl`、`启动安装.sh` 内容（仅用 `ls`/`test -f` 确认入口存在，不读脚本正文）；只看向导屏幕文字 + 手册。
- 时区：全部时间戳为 UTC（`date -u`）。

---

## 0. 预检（安装前，read-only）

| 项 | 命令 | 结果 | 手册条款 |
|---|---|---|---|
| 容器在线 | `docker ps --filter name=mm-cr-a2` | `Up`，镜像 `mm/kylin-v10-snapshot0:20260917` | 你需要准备（银河麒麟 V10） |
| 介质目录/入口脚本存在 | `ls` + `test -f 启动安装.sh` | 目录存在，`启动安装.sh EXISTS` | 步骤1、步骤2（无图形：`./启动安装.sh`） |
| 磁盘空间 | `df -h /` | 可用约 820 GB ≥ 6 GB | 你需要准备（≥6 GB） |

（本阶段仅 read-only 观察，未执行任何安装/改动动作。）

---

## 1. 向导逐屏记录

调用（一次性给全答案，未做任何"只回车试探"；答案顺序 = 安装位置 / 数据位置 / 端口 / 密码 / 确认）：

```
printf '\ny\n\nn\ny\n' | docker exec -i mm-cr-a2 bash -c 'cd "/root/物料集团码智能匹配平台安装包" && ./启动安装.sh' > testA2-wizard-full.log 2>&1
```

| # | 屏幕提示摘录（实际原文） | 我的作答 | 对应手册条款 | 结果 | 时间戳(UTC) |
|---|---|---|---|---|---|
| 屏0 | `== 欢迎 ==` 版本 1.1.18 架构 x86_64；`正在自动检查运行环境……` | 无输入（自动前进） | 步骤3.1 欢迎页/点击继续（终端为自动） | 自动进入环境检查 | 16:11:33 |
| 屏0b | `== 环境检查 ==` ✅操作系统 ✅CPU架构 ✅systemd ✅安装入口 ✅磁盘约819GB ✅离线环境与AI模型；`ℹ️ 未检测到已安装版本：本次为首次安装` | 无输入（自动） | 步骤3.2 环境检查逐项✅/❌，全✅才继续 | 全部✅，判定首次安装 | 16:11:33 |
| 屏1 | `程序安装位置（直接回车使用默认值） [/opt/material_matcher]：` | **回车**（默认） | 步骤3.3 安装位置 回车默认 `/opt/material_matcher` | 采用 `/opt/material_matcher` | 16:11:33 |
| 屏2 | `数据目录默认由安装程序自动选择空间最大的安全磁盘…（推荐）` `【继续】自动选择 /【取消】返回，并在下一屏手工指定数据目录 输入 y 继续，其它键取消：` | **y**（自动选择） | 步骤3.4 数据位置 推荐"自动选择" | 数据目录=自动选择安全盘 | 16:11:33 |
| 屏3 | `服务监听端口（升级安装建议保持默认=当前端口） [18080]：` | **回车**（默认18080） | 步骤3.5 服务端口 默认 18080 | 端口=18080 | 16:11:33 |
| 屏4 | `是否为 admin 设置初始密码？` `【继续】由您输入密码 /【取消】自动生成强密码（完成后显示一次，请务必保存） 输入 y 继续，其它键取消：` | **n**（其它键=自动生成） | 步骤3.6 密码"手工输入 或 选择自动生成"；选自动生成 | 采用自动生成强密码，写入 root-only 文件，不回显 | 16:11:33 |
| 屏5 | `即将开始安装，请确认：` 产品/版本1.1.18(commit 169d511…)/架构/方式=首次安装/程序目录=/opt/material_matcher/数据目录=自动/端口=18080/管理员密码=自动生成（不回显）/网络=完全离线 …`输入 y 继续，其它键取消：` | **y**（确认开始） | 步骤3.7 确认安装 核对后继续 | 开始安装 | 16:11:33 |
| 进度 | `[1%]…[100%] 安装完成` 各阶段文字：校验介质/复制程序/安装模型/环境诊断/配置systemd/启动服务/生成报告 | — | 步骤3.8 安装进度 每阶段文字提示 | 全阶段完成 | 16:11:33→16:11:42 |
| 完成页 | `【安装成功】物料集团码智能匹配平台 1.1.18`；地址 `http://127.0.0.1:18080`；`初始密码已自动生成…本输出与安装日志不包含密码明文；请…root 查看 /etc/material_matcher/secret/admin_password.env`；`安装报告 /var/log/material_matcher/install-reports/install-20260917-161141.txt` | — | 步骤3.6"输出被重定向到日志/管道（非交互终端）密码不显示，请root查密码文件"；步骤4 成功页 | 与手册"重定向则不回显密码"条款**完全吻合** | 16:11:42 |

- 向导退出码 RC=0。
- 向导过程日志：`/var/tmp/material_matcher_wizard-20260917-161133.log`（符合手册"失败时"给出的默认日志路径 `/var/tmp/material_matcher_wizard-*.log`）。

---

## 2. 密码文件确认（步骤6，禁写明文）

| 检查 | 命令（不打印明文值） | 结果 |
|---|---|---|
| 存在 | `test -f /etc/material_matcher/secret/admin_password.env` | EXISTS |
| 权限 | `stat -c "owner=%U group=%G perms=%a"` | `root:root 600`（仅 root 可读，符合手册"安全说明"） |
| 非空 | `wc -c` | 45 字节（含取值） |

> 依禁令：未 `cat` 密码文件内容，未在任何记录中写入密码明文。

---

## 3. 最小验收（逐条）

| 手册条款 | 执行方式 | 结果 |
|---|---|---|
| 5.1 登录成功 | 无浏览器→由原厂 smoke `STEP0 admin 登录` + `首次登录修改密码` + `新密码重新登录` 取证 | PASS（status=200） |
| 5.2 版本号与 BUILD_INFO.txt 一致 | `cat BUILD_INFO.txt`=**1.1.18**；`/api/health`=**1.1.18**；向导=1.1.18（commit `169d511dd01671ace8b86b54cce5cd6b0641dd2a` 与 BUILD_INFO 一致） | 一致 PASS（**浏览器"关于"项以 /api/health 代替**） |
| 5.3 STEP1 数据上传+启动匹配 | smoke（介质自带 `smoke/`）：上传待匹配/集团标准、建目录、建草稿、启动匹配 | PASS（均 200） |
| 5.4 STEP2 进度 / STEP3 结果 / STEP4 导出 | smoke：`STEP2 匹配完成 COMPLETED 用时=1s`、`STEP3 打开结果明细 200`、`STEP4 生成结果/导出清单/下载结果 Excel bytes=14871` | PASS |
| 5.5 重启后服务自动恢复 | `docker restart mm-cr-a2`（代替物理重启），等待 15s | 见下 |

- 浏览器替代标注：环境无浏览器，手册"打开地址登录/关于/结果"等浏览器项 → 用 `curl /api/health`、`/api/health/ready` 及原厂 `installer_smoke.py` 代替。
- 原厂工具（手册外允许项）：`installer_smoke.py` 为介质自带官方验收工具，任务明确列为允许。

### 健康检查（浏览器代替，安装后即测）
```
/api/health       -> {"status":"ok","version":"1.1.18"}                 [http_code=200]
/api/health/ready -> {"status":"ready","checks":{admin_account,database,data_dir,tmp_dir=true}}  [http_code=200]
```

### 重启自动恢复（docker restart 代替物理重启，标注）
```
docker restart mm-cr-a2 rc=0; sleep 15
systemctl is-active  material_matcher -> active
systemctl is-enabled material_matcher -> enabled
/api/health        -> {"status":"ok","version":"1.1.18"}   [200]
/api/health/ready  -> {"status":"ready",…true}             [200]
```
> 说明：物理重启不可用，按任务用 `docker restart` 等价替代（15 秒 < 手册"约1分钟"，服务在 15s 内已就绪并回 200）。

---

## 4. 耗时统计（UTC）

| 阶段 | 起 | 止 | 用时 |
|---|---|---|---|
| 向导（交互+安装，全程） | 16:11:33 | 16:11:42 | **9 s**（含 RC=0 成功页） |
| 版本/密码文件核对 | ~16:11:45 | ~16:12:20 | ~35 s（人工读取记录） |
| 健康检查 curl | 16:12:23 | 16:12:23 | ~1 s |
| 原厂 smoke（STEP0–4） | 16:12:28 | 16:12:30 | ~2 s（工具自报"17/17 passed in 1s"） |
| 重启恢复测试 | 16:12:41 | 16:12:58 | ~17 s（含固定 sleep 15s） |
| **总耗时（向导开始→重启确认通过）** | 16:11:33 | 16:12:58 | **85 s** |

> 手册称安装"通常需要 3～10 分钟"；本机（快照容器/缓存/快盘）实际仅 9 秒即完成，属"依服务器性能"允许范围内的加速，非缺陷。

---

## 5. 三个必答问题

**(a) 是否存在任何一次无输出超过 30 秒的"疑似卡死"？**
**否。** 向导整体墙钟 9 秒（16:11:33→16:11:42），内部无单段可超过 30s 静默；进度 1%→100% 连续推进（复制程序/安装模型/启动服务等均有即时行输出）。curl、smoke 均秒级返回；重启后 15s 内 health 即 200。全过程未观察到任何 >30 秒无输出的停顿。

**(b) 是否执行过任何手册外命令？**
执行了手册正文之外的命令，但**均属 (i) 任务明确授权的环境等价替代 或 (ii) 只读校验，未对手册安装流程做任何改动型越权**：
- 授权替代项：`curl /api/health`、`/api/health/ready`（代替浏览器）；`installer_smoke.py`（原厂验收工具，STEP1–4）；`docker restart mm-cr-a2`（代替物理重启）。
- 只读校验项：`docker ps`、`ls`/`test -f` 介质与入口、`df`、`cat` 向导过程日志与 `BUILD_INFO.txt`、`stat`/`wc` 密码文件元数据（**未读密码值**）、`systemctl is-active/is-enabled`。
- 未执行：任何 `install.sh`/`mmctl`/`维护工具.sh` 的手工调用、任何删除/改配置/改防火墙等手册外写操作。
- 一处需记录的内部字段小瑕疵：结果 JSON `password_source":"user"` 与界面"自动生成"标签措辞不一致（界面确认页明确显示"自动生成"，功能正确；该字段为内部实现标签，不属手册范围，不影响验收）。

**(c) 向导文字与手册是否逐项一致？（不一致处逐条列出）**
逐项核对，**核心内容全部一致**；仅以下为终端向导与手册（面向图形界面）在措辞/交互形式上的差异，**均非内容冲突**：
1. 手册写"点击【继续】/双击"，终端向导改为"回车 / 输入 y 继续，其它键取消"——交互形式差异（无图形，属预期）。
2. 环境检查多出一行 `ℹ️ 未检测到已安装版本：本次为首次安装`（手册未列，为附加信息，不冲突）。
3. 端口提示文案含"升级安装建议保持默认=当前端口"，而本次为首次安装——通用提示语，默认值 `[18080]` 与手册一致，不冲突。
4. 密码屏语义方向：手册"手工输入 或 选择自动生成"；终端把"【继续】=手工输入 / 【取消/其它键】=自动生成"，即**选"自动生成"须按非 y 键**。与手册一致，但方向需按屏文字判断（我按屏文字作答 `n` → 确认页显示"管理员密码：自动生成"，选择正确）。
5. 数据屏【取消】提示"返回并在下一屏手工指定"，在非交互管道下无实际"下一屏返回"；本次走【继续】自动，未触发，不冲突。
6. 手册 3.6"自动生成时安装完成页会显示一次"，本次因输出重定向到管道，完成页改为"密码不在屏幕/日志显示，请 root 查密码文件"——此为手册 3.6 **同一条款的另一半**（重定向场景），行为与手册规定完全吻合。

结论：向导文字与手册在版本、目录、端口、密码策略、进度阶段、成功页、日志路径、安全说明等关键项**逐项一致**；差异仅为图形 vs 文本的呈现形式与个别附加提示，无实质不符。

---

## 6. 最终结论

**PASS。**
- 首次安装：向导 RC=0，【安装成功】1.1.18，服务 active+enabled，端口 18080。
- 版本一致：BUILD_INFO.txt / 向导 / `/api/health` 均 1.1.18，commit 一致。
- 最小验收：登录/STEP1/STEP2/STEP3/STEP4 由原厂 smoke 17/17 全通过（含结果 Excel 下载）；健康与就绪接口 200。
- 重启自动恢复：`docker restart` 后 15s 内服务恢复、health/ready 200。
- 密码：root-only 600 文件存在、不回显、未写入任何明文。
- 无 >30s 疑似卡死；手册外命令仅为授权替代/只读校验；向导文字与手册逐项一致（差异均为图形↔文本呈现形式，无内容冲突）。
