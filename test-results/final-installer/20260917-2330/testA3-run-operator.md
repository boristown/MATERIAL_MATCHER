# MATERIAL_MATCHER 验收安装记录（测试 A3 / 独立安装员）

- 日期：2026-09-18（宿主机时钟；容器内为 UTC+0 显示 23:33 段）
- 目标机：docker 容器 `mm-cr-a3`（银河麒麟 V10 镜像，x86_64，root，完全断网，无图形桌面）
- 唯一参考：`manual-copy-安装手册-v3.md`（安装手册 v3）。未读取 MATERIAL_MATCHER 仓库任何文件；未查看 install.sh / install_wizard.sh / mmctl / 验收脚本内容。
- 介质：`/root/物料集团码智能匹配平台安装包`（步骤 1 已由环境预先完成）
- 完整屏幕输出：`testA3-wizard-full.log`（逐行时间戳版：`testA3-wizard-timeline.log`）

## 执行过程（逐屏记录）

### 预备：介质核对 07:32:50
`ls` 介质顶层，确认 `启动安装.sh`、`BUILD_INFO.txt`、`smoke/` 存在（手册步骤 1/2、5.2 涉及文件）。未打开任何脚本内容。

### 试探：空输入运行 07:33:0x（无键入，向导在确认屏 EOF 自动取消，“未对系统做任何修改”）
目的：在正式一次给全按键前，按“屏幕实际显示文字”核对手册 3.x 条款映射。不启动安装，不留系统改动。
屏幕关键文字（与手册条款映射）：
| 屏幕提示摘录 | 手册条款 | 对应手法 |
|---|---|---|
| `== 欢迎 ==`…`正在自动检查运行环境……`（无独立“继续”按键提示） | 3.3.1 欢迎页—点击继续 | 终端分支自动前进（见问题 1） |
| `== 环境检查 ==` 全部 ✅，`ℹ️ 未检测到已安装版本：本次为首次安装` | 3.3.2 环境检查自动、全✅才继续 | 无需按键 |
| `程序安装位置（直接回车使用默认值） [/opt/material_matcher]：` | 3.3.3 直接回车默认 | **回车** |
| `主按钮 / 输入 y …… 自动选择 副按钮 / 输入其它 …… 手工指定` | 3.3.4 推荐“自动选择” | **y** |
| `服务监听端口（首次安装默认 18080，直接回车即可） [18080]：` | 3.3.5 默认 18080 | **回车** |
| `主按钮 / 输入 y …… 手工输入密码 副按钮 / 直接回车 …… 自动生成强密码（推荐）` | 3.3.6 手工或自动生成 | **回车=自动生成**（管道非交互下手册明示密码不显示、事后 root 查密码文件；管道内传手工明文密码违反安全说明，故选自动生成） |
| `即将开始安装，请确认：…输入 y 继续，其它键取消：` | 3.3.7 核对版本/目录/端口后继续 | **y** |

### 正式安装运行 07:33:08 – 07:33:14（宿主机时间）
输入序列（一次给全）：`\n y \n \n y \n`（+1 个收尾回车）。
- 安装位置：回车 → 默认 `/opt/material_matcher` ✅
- 数据目录：y → 自动选择（确认页显示“自动选择安全数据盘”）✅
- 端口：回车 → 18080 ✅
- 密码：回车 → 自动生成；屏幕明示“输出与日志不含密码明文，请 root 查看 /etc/material_matcher/secret/admin_password.env”（与手册 3.3.6、安全说明一致；本记录不写任何密码值）
- 确认：y → 安装进度 1%→100%，各阶段均有文字提示（校验介质→复制程序→安装模型→环境诊断→配置 systemd→启动服务→安装报告），符合手册 3.3.8 所列阶段
- 结果页：`【安装成功】…1.1.19`，给出地址 `http://127.0.0.1:18080`、账号 admin、密码文件路径、安装报告路径 `/var/log/material_matcher/install-reports/install-20260917-233314.txt`；过程日志 `/var/tmp/material_matcher_wizard-20260917-233308.log`（手册 3.3.6 完成页指引、失败日志路径条款均兑现）

阶段耗时：
| 阶段 | 时间 | 耗时 |
|---|---|---|
| 环境检查+交互选择 | 07:33:08–07:33:08 | <1s |
| 校验介质 | 07:33:08–07:33:11 | 3s |
| 复制/模型/诊断/服务启动 | 07:33:11–07:33:14 | 3s |
| 向导总计 | — | **6s**（手册称“通常 3～10 分钟”，本次容器磁盘极快，属性能差异，不判不一致） |

### 步骤 4 验证（浏览器替代表）07:33:25
- `curl /api/health` → 200 `{"status":"ok","version":"1.1.19"}`
- `curl /api/health/ready` → 200 `admin_account/database/data_dir/tmp_dir 全 true`
- `GET /` → 200（575 字节）
- 版本核对（手册 5.2“关于=BUILD_INFO”替代）：API 版本 1.1.19 + 向导确认页 commit `e843e4ad…` 与 `BUILD_INFO.txt`（版本 1.1.19、同 commit）一致 ✅
- 密码文件（手册安全说明）：`ls -l` 显示 `-rw------- root:root /etc/material_matcher/secret/admin_password.env`（600）✅（未 cat、未落盘明文）

### 步骤 5 最小业务验收（原厂 installer_smoke.py，替代表授权）
- 第 1 次 07:33:28：`PASS 登录`；`FAIL STEP0 首次登录修改密码 :: status=422`；`FAIL STEP0 新密码重新登录 :: 401` → 3/5
- 第 2 次 07:33:37：**17/17 PASS**（登录、首次改密 200、新密码重登、前端可达、STEP1 上传两组数据+建草稿+启动匹配、STEP2 `COMPLETED 用时=1s`、STEP3 结果明细 200、STEP4 生成/导出清单/下载 Excel 14873 字节）
- 手册 5.3/5.4（上传《最小测试数据》、方案匹配、进度、结果、导出）经该工具等价覆盖 ✅；首次运行 422 见问题 3。

### 步骤 5.5 重启恢复（`docker restart mm-cr-a3`，替代表授权）
- 07:33:49 restart → 07:34:05（16s，快于手册“约 1 分钟”）
- 重启后 health/ready/GET / 全部 200，版本 1.1.19 ✅

## 三个必答问题

**(a) 是否出现单次超过 30 秒无输出（疑似卡死）？**
**无。** 逐行时间戳显示最长静默 3 秒（介质校验 07:33:08→07:33:11）；重启等待 15 秒为替代表规定动作。

**(b) 是否执行了手册未涵盖且未经替代表授权的命令？**
基本无。超出两者字面范围的只有介质顶层 `ls -la`（只读清点，用于确认手册步骤 1/2 前提）；其余全部命令逐条对应手册条款（`./启动安装.sh` 终端分支、BUILD_INFO 核对、密码文件 root 查验）或用户批准的等价替代表（curl、smoke、docker restart、sleep）。密码文件仅 `ls -l`/`stat` 验证存在与权限，未读取内容。无其他任何安装介质脚本查看或额外系统命令。

**(c) 向导文字与手册是否逐项一致？**
大体一致（页序、默认值、端口 18080、默认目录、密码文件路径、日志路径、成功页要素全部吻合）。不一致/瑕疵逐条如下：
1. 手册 3.3.1“欢迎页——点击继续”：终端向导欢迎页无独立“继续”按键提示，直接自动进入环境检查（图形/终端措辞差异，不阻碍操作）。
2. 数据目录屏通用按钮文案“输入 y 继续，其它键取消”与该屏实际行为矛盾：手册推荐的“自动选择”=y，但按回车/其它键并非“取消”，而是进入“手工指定数据目录（高级）”路径输入提示。屏幕自身两行说明（副按钮=手工）与末行通用文案（其它键=取消）互相冲突。
3. 手册 3.3.8 称安装“通常需要 3～10 分钟”：本次 6 秒完成。因手册限定为“根据服务器性能通常”，判为性能差异，不计不一致（仅记录）。
4. 手册步骤 4/5.1 假定浏览器与界面登录；本容器无浏览器，已按批准替代表（curl + 原厂 smoke）等价验证，属环境替代表范围，不计向导不一致。

## 问题列表（验收瑕疵）
1. 数据目录屏文案自相矛盾（见 (c)2）：误按回车不会取消、而是滑入高级手工分支，可能误导普通安装员。
2. 原厂冒烟第 1 次运行 `STEP0 首次登录修改密码` 返回 422（新密码重登随之 401），第 2 次运行同一操作 200 通过——首次运行非幂等/疑似竞态，建议原厂复查首登改密接口的偶发校验失败。
3. 欢迎页“点击继续”在终端分支无对应按键（仅措辞差，见 (c)1）。

## 时间线汇总
- 07:32:50 介质核对｜07:33:0x 空输入试探（自动取消、零改动）｜07:33:08–07:33:14 正式安装（6s）｜07:33:25 健康+密码文件+BUILD_INFO 核对｜07:33:28 冒烟#1（FAIL 422）｜07:33:37 冒烟#2（17/17 PASS）｜07:33:49 重启｜07:34:05 重启后验证全 200
- **总耗时：约 1 分 15 秒**（其中向导 6 秒）

## 结论
**PASS（有保留项）**：安装向导可被未参与开发的安装员仅凭手册独立完成，安装成功、服务就绪、业务链路（上传→匹配→进度→结果→导出）经原厂工具 17/17 通过、重启后自动恢复；密码文件 600/root 符合安全说明。保留项为上述问题 1、2（文案矛盾与首登改密接口偶发 422），不影响本次验收安装结果成立。
