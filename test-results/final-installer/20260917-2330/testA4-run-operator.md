# MATERIAL_MATCHER 离线安装 · 正式验收安装（最终轮）操作记录 — 测试 A4

- 执行时间：2026-09-18 07:49:54 ～ 07:51:08（宿主 CST；容器内为 UTC，相差 8 小时）
- 执行人角色：未参与本项目开发的运维安装员
- 唯一参考资料：`manual-copy-安装手册-v4.md`（安装手册 v4，45 行）
- 目标环境：docker 容器 `mm-cr-a4`，Kylin Linux Advanced Server release V10 (Lance)，x86_64，
  完全断网、无图形桌面、无浏览器；`/` 可用 843 GB
- 安装介质（已按手册第 1 步复制到本地磁盘）：`/root/物料集团码智能匹配平台安装包`
  介质 `BUILD_INFO.txt` 版本：`1.1.20`，架构 x86_64
- 起始状态：`/opt` 为空、无 `/etc/material_matcher`、服务 inactive、18080 端口空闲 → 首次安装
- 纪律遵守：未读取 MATERIAL_MATCHER 仓库任何文件；未查看 `install.sh` / `install_wizard.sh` /
  `mmctl` / `verify_offline_bundle.py` 内容；仅依据手册文字与屏幕提示行事
- 附件：`testA4-wizard-full.log`（完整屏幕输出）、`testA4-wizard-timeline.log`（逐块输出到达时间，用于静默期分析）

## 一、依据手册预先规划的每一屏答案（一次给全）

手册第 3 步共 8 屏，其中 1、2、7、8 屏终端无需输入（欢迎自动进入检查、环境检查自动、
安装进度自动），需输入的 4 屏规划如下：

| 屏幕 | 手册依据 | 规划答案 |
|---|---|---|
| 3 安装位置 | 直接回车使用默认 `/opt/material_matcher` | 回车（空行） |
| 4 数据位置 | 输入 y 自动选择（推荐） | `y` |
| 5 服务端口 | 默认 18080 | 回车（空行） |
| 6 管理员密码 | y 手工输入 / n 自动生成 | `n`（自动生成，避免把明文密码写入命令行或日志） |
| 7 确认安装 | 核对后确认（终端按屏幕提示输入 y/n） | `y` |

实际发送：`printf '\ny\n\nn\ny\n'`（共 5 行输入，未多未少，一次给全，未做任何追问式补喂）

命令（手册第 2 步「无图形界面」分支 + 允许的技术方式）：
```
printf '\ny\n\nn\ny\n' | docker exec -i mm-cr-a4 bash -c \
  'cd "/root/物料集团码智能匹配平台安装包" && ./启动安装.sh' 2>&1 | tee testA4-wizard-full.log
```

## 二、向导逐屏实际表现与手册对照

| # | 手册描述 | 屏幕实际 | 一致性 |
|---|---|---|---|
| 1 欢迎页 | 终端显示欢迎文字后自动开始环境检查 | 显示「== 欢迎 ==」文字、版本 1.1.20、架构 x86_64 后输出「正在自动检查运行环境……」，无需输入 | 一致 |
| 2 环境检查 | 自动完成，逐项 ✅/❌，全 ✅ 才继续 | 6 项全 ✅（操作系统/CPU 架构/systemd/入口文件/磁盘 843 GB/离线环境与模型），另有一行手册未提及的 `ℹ️ 未检测到已安装版本：本次为首次安装` | 一致（多 1 行信息提示，良性） |
| 3 安装位置 | 直接回车用默认 `/opt/material_matcher` | 提示「（直接回车使用默认值）[/opt/material_matcher]」，回车即默认 | 一致 |
| 4 数据位置 | y 自动选择（推荐），n 手工指定（高级） | 提示文字与选项完全对应，并额外说明「通过 /var/lib/material_matcher 统一访问」；其它键退出提示与手册第 3 步总则一致 | 一致 |
| 5 服务端口 | 默认 18080；被占用时提示改选 | 提示「首次安装默认 18080，直接回车即可」；本机端口空闲，占用改选分支本轮未触发、无法实测 | 一致（占用分支未覆盖） |
| 6 管理员密码 | y 手工输入（≥10 位、含字母数字、不回显）或 n 自动生成；输出被重定向到管道时密码不显示、只写入 root-only 文件 | 选 `n`；确认页显示「自动生成（写入 root-only 密码文件，不回显）」；成功页明确「本输出与安装日志不包含密码明文」；实测 `admin_password.env` 为 `-rw------- root:root`，向导日志与安装报告中该密码明文出现次数均为 0 | 一致 |
| 7 确认安装 | 核对版本、目录、端口后点击「继续」 | 终端列出 产品/版本 1.1.20+commit/架构/方式/程序目录/数据目录/端口 18080/密码方式/网络，并要求「请输入 y = 开始安装；n = 取消安装」；与 BUILD_INFO 版本一致 | 基本一致（手册此处只写图形「点击继续」，未写明终端需输 `y`，依赖第 3 步总则推断） |
| 8 安装进度 | 每阶段有文字提示（校验介质、复制程序、安装模型、环境诊断、启动服务、验证就绪等），通常 3～10 分钟 | 百分比阶段提示齐全：1% 准备 → 3% 校验介质 → 5/6/8/10/12% 检查 → 18% 复制程序 → 40% 安装 AI 模型 → 44/45/46/48% → 55% doctor → 60% systemd → 66% 启动并等待就绪 → 96% 报告 → 100%；退出码 0；结束显示日志路径 `/var/tmp/material_matcher_wizard-20260917-234955.log`（与手册默认路径一致）与安装报告路径 | 一致；两处轻微问题见「问题列表」 |
| 4 安装成功页 | 见【安装成功】，按提示浏览器打开地址，admin 登录，首登改密 | 显示「============== 安装成功 ==============」、地址 `http://127.0.0.1:18080`、账号 admin、密码文件获取方式、安装报告路径、首登需改密提示、后续维护工具提示 | 一致（浏览器环节以 curl 等价验证） |

安装总耗时（屏幕首次输出 → 向导结束）：**约 6.5 秒**（07:49:54.1 → 07:50:00.6），
远小于手册标称的 3～10 分钟（差异原因见问题列表，不影响功能验收）。

## 三、必答三问

### (a) 是否存在单次超过 30 秒无输出？
**不存在。** 依据 `testA4-wizard-timeline.log` 对 31 个输出块到达时刻逐块计时：
最大静默间隔 **3.09 秒**，出现在 `[3%] 正在校验安装介质完整性` 阶段（16571 个文件 SHA256 校验）；
次大 1.08 秒（`[66%] 启动服务并等待就绪` → `[96%] 生成安装报告`）。
全程无任何阶段超过 30 秒；亦无「阶段之间静默但无提示」的情况（66%→96% 阶段跳号但间隔仅约 1.1 秒，且有「等待就绪」文字在前）。

### (b) 是否存在等价替代表之外的额外命令？
**安装与最小验收本身：无**（严格只用手册明示的终端 `./启动安装.sh` 分支 + 表内 4 项等价替代）。
为完成记录与安全核对，额外执行了以下**只读**命令，均已标注：
- `cat BUILD_INFO.txt`、`ls`（介质/`smoke/`/目录与文件权限）— 手册第 5.2 步要求「与介质 BUILD_INFO.txt 一致」，必须读取；
- `systemctl is-active / is-enabled material_matcher` — 服务状态核对（未使用 `mmctl`，未查看其内容）；
- `grep` 统计密码明文在向导日志/安装报告中的出现次数（只输出计数，不输出密码）；
- 宿主侧行到达时间戳过滤器 `tstamp.py`（题目允许的交互技术方式，与安装知识无关）。

### (c) 向导文字与手册逐项是否一致？
**8 屏逐项对照全部一致**（见第二节表格）：屏幕出现的输入引导语（直接回车 / y / n / 其它键退出）
与手册描述可一一对应，预规划的答案一次给全即顺利完成，未被要求补充答案、未出现手册未说明的额外提问屏。
仅存在以下措辞/呈现层面的偏差（不影响可操作性），全部列入「问题列表」。

## 四、最小业务验收（等价替代）

| 手册要求 | 等价做法与结果 |
|---|---|
| 浏览器打开地址登录 | 无浏览器 → `curl http://127.0.0.1:18080/api/health` = `{"status":"ok","version":"1.1.20"}` HTTP 200；`/api/health/ready` = `ready`（admin_account/database/data_dir/tmp_dir 全 true）HTTP 200；`GET /` HTTP 200，575 字节页面 |
| 5.1 登录成功；首登改密 | 原厂工具 `tools/installer_smoke.py`：`STEP0 admin 登录` PASS、`STEP0 首次登录修改密码` PASS(200)、`STEP0 新密码重新登录` PASS(200) |
| 5.2 版本号与 BUILD_INFO 一致 | 介质 BUILD_INFO `1.1.20` = `/api/health` version `1.1.20` = 向导确认页/成功页/安装报告 `1.1.20`（commit `8e40d4f1…` 亦一致）；「系统设置·关于」页面以版本接口等价核对 |
| 5.3 数据上传 + 选择匹配方案启动匹配 | smoke 工具：上传待匹配数据 / 集团标准数据 / 建立标准数据目录 / 创建任务草稿 / 启动匹配 全 PASS(200)。注：介质 `smoke/` 实际文件名为 `smoke-待匹配数据.xlsx`、`smoke-集团标准数据.xlsx`，**不存在手册所称《最小测试数据.xlsx》** |
| 5.4 第二步进度、第三步结果、第四步导出 | `STEP2 匹配完成 status=COMPLETED 用时=1s`、`STEP3 打开结果明细 200`、`STEP2/3 进度与结果可见`、`STEP4 生成结果 200`、`STEP4 导出清单`（返回 download_url）、`STEP4 下载结果 Excel bytes=14872` 全 PASS |
| 合计 | **SMOKE 17/17 passed in 1s**（工具退出码 0） |
| 5.5 重启后服务自动恢复 | `systemctl is-enabled` = `enabled`（安装期已创建 multi-user.target 自启符号链接）；`docker restart mm-cr-a4` 后等 15 秒：服务 `active`，`/api/health` 200（version 1.1.20）、`/api/health/ready` 200、`GET /` 200（手册称「等待约 1 分钟」，实测 15 秒内已就绪） |

## 五、安全与失败处理相关核对

- 密码文件 `/etc/material_matcher/secret/admin_password.env`：`-rw------- root root`，符合手册第 6 步与安全说明；
- 向导日志 `/var/tmp/material_matcher_wizard-20260917-234955.log`（root-only，83 行）与安装报告
  `/var/log/material_matcher/install-reports/install-20260917-235001.txt`（root-only）中，密码明文出现次数均为 **0**；
  本记录与屏幕输出文件均不含任何敏感值；
- 手册承诺的日志路径、安装结束显示路径 —— 一致；
- 「安装失败」路径、`维护工具 → 导出诊断包`、端口占用改选、非法输入「安全退出不做任何修改」
  本轮为成功的全新安装，**未覆盖**（如需覆盖应另做破坏性子轮，避免影响本验收环境）。

## 六、问题列表（本轮发现，按严重度）

| # | 严重度 | 问题 |
|---|---|---|
| 1 | 中 | 手册第 5.3 步要求上传介质 `smoke/` 目录中的《最小测试数据.xlsx》，实际介质内为 `smoke-待匹配数据.xlsx` 与 `smoke-集团标准数据.xlsx`，无该文件名；照手册找文件的安装人员会卡住 |
| 2 | 轻 | 手册第 3.7 步「确认安装」只写「点击继续」，未写终端需输入 `y`（仅在总则里笼统说 y/n），纯终端用户需自行推断 |
| 3 | 轻 | 手册第 3.8 步称「每个阶段都有文字提示」，实测 `[66%]` 直接跳到 `[96%]`，中间（启动服务、验证就绪）无逐条输出；且 `[3%] 校验介质` 阶段打印原始 JSON（`{"file_count":16571,…}`），对无开发背景管理员不可读 |
| 4 | 轻 | 手册称安装「通常需要 3～10 分钟」，实测 6.5 秒完成（本环境为预热容器/快照镜像），差距悬殊，易让真实用户误判异常；成功页地址仅显示 `http://127.0.0.1:18080`，未按手册示例给出局域网可分享地址 |
| 5 | 轻 | 容器内时间为 UTC，日志文件名/报告时间（23:49/23:50）与宿主墙钟（07:49/07:50）相差 8 小时，手册未提示，按时间找日志易困惑 |
| 6 | 观察 | 环境检查多出一行手册未提及的 `ℹ️ 未检测到已安装版本：本次为首次安装`（良性）；失败处理、诊断包导出、端口占用、非法输入退出、升级安装路径本轮未验证 |

## 七、结论

**PASS**

- 仅凭手册 v4 与屏幕提示，预规划 5 个答案一次给全，向导一次走通并显示【安装成功】，退出码 0；
- 无单次超过 30 秒的静默（最大 3.09 秒）；
- 8 屏向导文字与手册逐项一致（存在 6 条记录在案的措辞/文档层面偏差，其中 1 条中等：smoke 数据文件名与手册不符）；
- 最小业务验收 17/17 通过（含登录、首登改密、上传、匹配、进度、结果、导出 Excel）；
- 服务开机自启，`docker restart` 后 15 秒内健康检查全部 200；
- 密码仅存 root-only 文件，日志与报告中零明文。
- 建议：修订手册第 5.3 步的文件名（问题 1）及第 3.7 步补充终端 `y` 说明（问题 2）后即可交付。
