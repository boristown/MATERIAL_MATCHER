# MATERIAL_MATCHER 首次安装 · 操作员实测记录 (Test A)

- 角色：未参与项目开发的运维安装员
- 唯一参考资料：`manual-copy-安装手册.md`（未阅读任何仓库源码 / install.sh / install_wizard.sh）
- 目标环境：docker 容器 `mm-cr-a`（模拟银河麒麟 V10 / x86_64）
- 安装介质：容器内 `/root/物料集团码智能匹配平台安装包`
- 完整终端输出：`testA-wizard-full.log`；验收输出：`testA-verify.log`
- 记录日期：2026-09-17

## 结论：**PASS**

手册“安装步骤 1–5（含最小验收 5 项）”全部满足；无手册外需求（除任务规则明确授权的“无浏览器→curl 代验”与“原厂 smoke 工具取证”两处，已在下文标注）。

---

## 操作前环境确认

| 时间 | 检查 | 结果 | 手册依据 |
|---|---|---|---|
| 23:48 | `docker ps` / `ls` 介质目录 | mm-cr-a Up；介质含 `启动安装.sh`、BUILD_INFO.txt、smoke/、tools/ 等 | §准备 / 第1步（介质已复制到位） |

> 说明：仅 `ls` 列目录名确认介质存在与 `启动安装.sh` 存在，未打开任何脚本内容。

---

## 向导逐屏问答（每一屏均先对照手册章节再作答）

无图形界面，按手册第 2 步“无图形界面：在终端执行 `./启动安装.sh`”启动。

| # | 屏幕提示（原文摘录） | 手册映射 | 我的作答 | 是否符合手册 |
|---|---|---|---|---|
| 1 | `== 欢迎 ==`（自动流到环境检查，无按键） | §3.1 欢迎页/继续 | 无需输入 | ✅ |
| 2 | `== 环境检查 ==` 全部 ✅，`ℹ️ 未检测到已安装版本：本次为首次安装` | §3.2 环境检查自动完成 | 自动 | ✅ |
| 3 | `程序安装位置（直接回车使用默认值） [/opt/material_matcher]：` | §3.3 安装位置 | 回车（默认 `/opt/material_matcher`） | ✅ |
| 4 | `【继续】自动选择 /【取消】手工指定数据目录 / 输入 y 继续` | §3.4 数据位置（推荐自动） | `y`（自动选择安全数据盘） | ✅ |
| 5 | `服务监听端口 [18080]：` | §3.5 端口默认 18080 | 回车（默认 18080，未见占用提示） | ✅ |
| 6 | `是否为 admin 设置初始密码？【继续】手工输入 /【取消】自动生成强密码` | §3.6 密码可选自动生成 | `n`（选自动生成） | ✅ |
| 7 | `即将开始安装，请确认：版本1.1.17/端口18080/数据目录自动/密码自动生成 … 输入 y 继续` | §3.7 确认页核对后继续 | `y` | ✅ |
| 8 | `开始安装（进度日志 /var/tmp/material_matcher_wizard-*.log）` | §3.8 安装进度 | 等待 | ✅ |

确认页核对：产品/版本 1.1.17/架构 x86_64/首次安装/程序目录 `/opt/material_matcher`/端口 18080/密码自动生成 —— 与手册及 BUILD_INFO.txt 一致。

---

## 安装进度阶段（来自 testA-wizard-full.log，进度 1%→100% 连续输出）

校验介质(16571 文件, ok) → 检查 OS/磁盘/数据目录/端口 → 创建服务账号 → 复制程序(~300MB) → 安装 AI 模型(~400MB, BAAI/bge-base-zh-v1.5) → 维护工具 → 前端离线重建资源 → 设置权限 → 环境诊断 doctor → 配置 systemd（创建 material_matcher.service 自启软链）→ 启动并等待就绪 → 生成安装报告 → **安装成功**。

- 启动 `23:51:55` → 看到【安装成功】页 `23:52:00`，退出码 0。
- 手册称“通常 3~10 分钟”；本容器 I/O 极快，实际约 **5 秒** 完成，进度文字持续刷新。
- **卡顿检查**：全程无“>30 秒无输出”现象。

安装成功页（浏览器地址、账号、一次性密码）已出现；一次性密码在终端仅显示一次，**未写入任何日志/报告明文**（已对 testA-wizard-full.log 脱敏）。初始密码保存在手册指定的 root-only 文件 `/etc/material_matcher/secret/admin_password.env`（即“管理员渠道”），报告中标注为 `<自动生成密码，已按手册保存到管理员渠道>`。

---

## 最小验收（手册第 5 步，5 项）

> 环境无图形桌面/浏览器。按任务规则，手册中“浏览器打开地址/登录界面”类检查以 `curl /api/health`、`curl /api/health/ready` 代替，并用**介质自带的原厂 `tools/installer_smoke.py`**（覆盖 STEP1–4 全链路 + 首次登录改密）取证。这两处替代/额外工具均在此如实标注。

| 验收项 | 手册条目 | 证据 | 结果 |
|---|---|---|---|
| 登录成功 | §5.1 | smoke `STEP0 admin 登录` / `首次登录修改密码 status=200` / `新密码重新登录 status=200` | ✅ |
| 版本号一致 | §5.2 | `/api/health` version=1.1.17 == BUILD_INFO.txt 1.1.17 | ✅ |
| 上传+选方案启动匹配 | §5.3 | smoke `STEP1 上传待匹配/标准数据(200)`、`建立标准数据目录`、`创建任务草稿`、`启动匹配(200)` | ✅ |
| 进度/结果/导出 | §5.4 | smoke `STEP2 匹配完成 COMPLETED 用时=1s`、`STEP3 打开结果明细(200)`、`STEP4 生成/导出/下载结果 Excel bytes=14872` | ✅ |
| 重启后服务自动恢复 | §5.5 | `docker restart mm-cr-a`（模拟重启服务器）→ 等待 60s → `/api/health` ok；`systemctl is-active`=active，`is-enabled`=enabled | ✅ |

- smoke 汇总：**17/17 passed**，退出码 0。
- 服务重启段：`23:53:17` restart → 等待 60s → `23:54:18` 确认恢复，health ok。
- `23:52:39` curl 健康检查：`/api/health` = `{"status":"ok","version":"1.1.17"}`；`/api/health/ready` = `{"status":"ready","checks":{admin_account/database/data_dir/tmp_dir: true}}`。

---

## 时间线与耗时

| 时刻 | 事件 |
|---|---|
| 23:48:29 | 第 1 次尝试：命名管道驱动向导（fd 未跨调用存活）→ 向导读到 EOF 安全自取消，**未对系统做任何修改** |
| 23:49:18 | 第 2 次尝试：同上管道方式，容器进程随工具超时被回收 → 向导停在输入位置，**未修改系统** |
| 23:49:38 | 改用手册/任务授权的“printf 应答 | docker exec -i”管道法 |
| 23:51:55 | 向导正式启动并逐屏应答 |
| 23:52:00 | **安装成功**（退出码 0）；安装净耗时 ≈ 5 秒 |
| 23:52:39 | 健康检查 + 原厂 smoke（17/17）|
| 23:52:40 | smoke 完成 |
| 23:53:17 | §5.5 重启容器 |
| 23:54:18 | 重启后服务恢复确认，验收结束 |

- 阶段耗时估计：介质复制+安装 ≈ 5s（I/O 极快）；健康检查 ≈ 1s；smoke 全链路 ≈ 1s；重启恢复观察 60s（含手册规定等待）。
- 安装+验收净耗时（23:51:55→23:54:18）≈ **2 分 23 秒**。
- 含前期工具调试的总墙钟（23:48:29→23:54:18）≈ **5 分 49 秒**。

---

## 手册外需求 / 替代 / 问题清单

1. **【替代·授权】** 无浏览器：手册 §4/§5 的“浏览器打开并登录”以 `curl /api/health`、`/api/health/ready` 代替（环境无 GUI）。
2. **【额外工具·授权】** 手册未含 `tools/installer_smoke.py`；本次仅作 STEP1–4 取证使用（原厂 smoke），属安装手册之外的验收辅助，非安装操作。
3. **【模拟·对应§5.5】** 用 `docker restart mm-cr-a` 模拟“重启服务器”以验证自恢复；属手册要求的验收项实现方式。
4. **【问题·已解决·未影响系统】** 前两次尝试采用命名管道 + 常驻 fd 交互；工具会话在超时/切换时回收了后台进程与 fd，导致向导读到 EOF。向导在每处 y/N 以“非 y=取消/默认”处理并最终 `用户已取消，未对系统做任何修改`，随即改用 `printf 应答 | docker exec -i` 一次成功。此为操作员侧驱动方式问题，与手册/产品无关。
5. **【脱敏·记录侧】** 一次性密码在终端显示后，已用 sed 从 `testA-wizard-full.log` 抹去明文，仅保留占位符；本报告不含明文密码（sed 仅编辑本记录文件，未触碰目标系统）。
6. 安装向导自身：未出现 >30s 无输出的卡顿。

## 安全说明核对（手册）
- 密码文件 root-only：`/etc/material_matcher/secret/admin_password.env`（smoke 经 `--password-file` 读取，未回显）。
- 安装未改防火墙规则（本环境 curl 走 127.0.0.1:18080 正常）。
